# Performance Optimization Tasks

## Incident

- Symptom: opening or diffing an approximately 150 MB JSON file crashed GitComet.
- Status: the current performance suite now has two credible ~150 MB syntax-path proxy reproductions (`file_diff_syntax_prepare/file_diff_syntax_prepare_cold` and `file_diff_syntax_query_stress/nested_long_lines_cold`), but the exact original UI trigger is still unrecorded.
- Known gap (2026-04-06): repo history, loop artifacts, and local Codex notes only narrow the trigger to "opening or diffing" the file; the exact UI action is still unrecorded.
- Known shape gap (2026-04-06): the incident record only preserves approximate payload size. The original line count and whether the JSON was minified or pretty-printed are still unrecorded.
- Syntax-path note (2026-04-06): `.json` maps to `DiffSyntaxLanguage::Json` in text and diff views. Full-document views attempt `DiffSyntaxMode::Auto`; very large patch diffs fall back to `HeuristicOnly` above 4,000 rows, so syntax highlighting was likely active in some form but the exact mode still depends on the missing UI path.
- Proxy shape note (2026-04-06): the current runbook brackets the missing line-layout data with two synthetic ~150 MB surrogates: `12,200 x 12,288-byte` lines (~149,913,600 B) for syntax prepare and `3,072 x 49,152-byte` nested lines (~150,994,944 B) for query stress.
- Benchmark-language correction note (2026-04-06): until the Turn 18 harness fix, the two incident-path syntax proxies were still using Rust grammar over Rust-like synthetic lines via `src/lib.rs` and `// cold_*` suffixes. The same benchmark labels now use `DiffSyntaxLanguage::Json` plus JSON-shaped synthetic documents with JSON-preserving cold markers, so the earlier Rust-proxy syntax timings and `tree_sitter_*` baselines are superseded.
- Failure-mode note (2026-04-06): GitComet installs a panic hook in the normal UI entrypoint and writes panic reports to `$XDG_STATE_HOME/gitcomet/crashes/`, but no new panic log exists for the April 6 incident window. The same window also has no `coredumpctl` hit and no kernel OOM record. A targeted `journalctl --user USER_INVOCATION_ID=...` audit shows the incident scope only has a start line at `2026-04-06 16:34:36.791443` and the final `app-gnome-gitcomet-673256.scope` teardown at `2026-04-06 21:15:00.308060` with a `7.8G` memory peak; the short relaunch at `2026-04-06 21:15:39.237411` to `2026-04-06 21:16:04.689911` shows the same pattern. Neither scope has a recorded exit code, signal, watchdog, or GNOME shell "not responding" entry, so the exact exit mode is still unresolved between external termination and manual close after a severe hang or memory spike.
- Local-evidence exhaustion note (2026-04-06): persisted `session.json` state only stores repo lists and UI/window preferences, not the last viewed file or diff target. A direct audit of `UiSession` and `UiSessionFileV2` in `crates/gitcomet-state/src/session.rs` confirms the persisted schema has no field for a selected diff target, preview path, or last-opened file. A fresh `journalctl --user` scan around `2026-04-06 21:15` still only shows the `app-gnome-gitcomet-673256.scope` teardown with a `7.8G` memory peak, and a broader `~/.config`/`~/.cache`/`~/.local/{state,share}` search for `*gitcomet*` only surfaced IDE/project metadata plus the already-audited runtime dirs. Shell history contains only plain `./target/debug/gitcomet` launches without path arguments, `~/.local/share/recently-used.xbel` has no large-JSON or GitComet file entry near the incident window, and the largest surviving `.json`/`.jsonl` under the common local directories checked is only ~`80.9 MiB`. Checklist item 1 is therefore blocked on an external incident note or user recollection rather than on an uninspected local source.
- Rebaseline result (2026-04-06): `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` has now been rerun after the JSON-fixture correction with the documented `12,200 x 12,288-byte` override. The corrected Criterion mean is `1.5167 s` with 95% CI `1.5159 s` to `1.5175 s`. The Rust-side sidecar remains small at `alloc_bytes=17,456`, `alloc_ops=6`, `realloc_ops=3`, `net_alloc_bytes=1,624`, while the native parser sidecar fields now report `tree_sitter_alloc_bytes=66,563,480`, `tree_sitter_dealloc_bytes=152`, `tree_sitter_alloc_ops=658,807`, `tree_sitter_realloc_ops=1`, and `tree_sitter_net_alloc_bytes=66,563,328`.
- Existing-proxy confirmation (2026-04-06): `file_diff_syntax_query_stress/nested_long_lines_cold` was also rerun after the same JSON-fixture correction on the documented ~144 MiB `3,072 x 49,152-byte`, `192`-nesting proxy. The corrected Criterion mean is `2.0269 s` with 95% CI `2.0236 s` to `2.0300 s`. Its Rust-side sidecar remains modest at `alloc_bytes=96,744`, `alloc_ops=8`, `realloc_ops=15`, `net_alloc_bytes=12,632`, while the native parser sidecar fields now report `tree_sitter_alloc_bytes=643,268,552`, `tree_sitter_dealloc_bytes=388,440,200`, `tree_sitter_alloc_ops=4,165,639`, `tree_sitter_realloc_ops=1`, and `tree_sitter_net_alloc_bytes=254,828,352`.
- Cold-harness note (2026-04-06): the two large-JSON cold syntax proxies now build unique shared source in Criterion setup and only time/measure prepared-document work from shared `text` plus `line_starts`, which matches the production call pattern more closely than rebuilding `Vec<String>` and rejoining full source inside the measured closure.
- Native-sidecar note (2026-04-06): the Criterion benchmark process now installs a benchmark-only tree-sitter allocator shim before registration and only enables native accounting during representative sidecar closures. Unprefixed `alloc_*` fields therefore still reflect Rust-side `PerfTrackingAllocator` metrics, while new `tree_sitter_alloc_*` sidecar fields now capture native parser heap from the same exact syntax workload without reintroducing the old benchmark-local source-assembly artifact.
- Artifact-integrity note (2026-04-06): filtered Criterion runs were able to leave `estimates.json` from one run while overwriting `sidecar.json` from another. The benchmark sidecar writer now honors the active CLI filter, and a follow-up `file_diff_syntax_prepare/file_diff_syntax_prepare_warm` run confirmed the corrected cold sidecar remained unchanged. That filter safety remains in place after the later cold-harness cleanup above.
- Narrow-filter note (2026-04-06): the current rebaseline benches now also skip representative sidecar measurement closures when the bench id does not match the active Criterion filter. Exact runs for syntax prepare, query stress, diff refresh, and diff-open proxies are therefore narrow in both sidecar writes and extra post-benchmark workload.
- Broader-suite exact-run note (2026-04-06): the top-level `performance` bench now also skips entire benchmark modules whose selectors cannot match an active `--exact` filter. That selector map includes the shared-group modules (`branch_sidebar`, `history_cache_build`, and `window_resize_layout`) by full exact bench id, so exact filtered runs no longer execute unrelated representative sidecar workload before the selected benchmark starts. Regex and unfiltered runs still register the full suite by design.
- Exact-run verification note (2026-04-06): after that top-level gating change, an exact verification run for the shared-group path `window_resize_layout/history_50k_commits_diff_20k_lines` completed successfully at mean `889.70 us` with sidecar `alloc_bytes=91,058`, `alloc_ops=518`, and `net_alloc_bytes=54,194`. A follow-up exact rerun of `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` also stayed within noise of the corrected JSON baseline at mean `1.5297 s` with Rust-side sidecar `alloc_bytes=15,920`, `alloc_ops=5`, `net_alloc_bytes=88`, and unchanged native parser sidecar `tree_sitter_alloc_bytes=66,563,480`, `tree_sitter_alloc_ops=658,807`, `tree_sitter_net_alloc_bytes=66,563,328`. A matching exact rerun of `file_diff_syntax_query_stress/nested_long_lines_cold` also stayed within noise at mean `2.0350 s` with 95% CI `2.0291 s` to `2.0423 s`; its Rust-side sidecar moved only slightly to `alloc_bytes=96,072`, `alloc_ops=8`, `net_alloc_bytes=11,960`, while the native parser sidecar stayed unchanged at `tree_sitter_alloc_bytes=643,268,552`, `tree_sitter_alloc_ops=4,165,639`, and `tree_sitter_net_alloc_bytes=254,828,352`.
- Stage-comparison note (2026-04-06): row paging is not the dominant cost in the current suite. With the documented `GITCOMET_BENCH_FILE_DIFF_LINES=1200000` and `GITCOMET_BENCH_FILE_DIFF_WINDOW=200` override, `diff_open_file_inline_first_window/200` completed in `1.524 us` and `diff_open_file_split_first_window/200` in `4.372 us`; both sidecars reported only `alloc_bytes=90,846`. Code inspection confirmed those benchmarks measure first-window paging after provider construction, so `diff_refresh_rev_only_same_content/rebuild` was used as the diff-plan proxy. At `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000`, that rebuild path still completes in `32.310 ms` with Rust-side sidecar `alloc_bytes=67,108,960` over an ~82.3 MB combined old/new input. Against that corrected comparison point, the two JSON syntax-path proxies remain the first real timing outliers: syntax prepare is now `46.9x` slower than diff rebuild and query stress is `62.7x` slower. For allocation, diff rebuild is still the largest Rust-side `alloc_*` baseline, but the syntax proxies remain the first confirmed native parser allocation outliers through `tree_sitter_alloc_*`.
- Diff-refresh benchmark correction note (2026-04-07): Turn 25 corrected `diff_refresh_rev_only_same_content/rebuild` so the label now mirrors the synchronous `ensure_file_diff_cache` rebuild through document-source cloning, line-start indexing, `side_by_side_plan`, and split/inline provider construction before syntax refresh. The older ~`32 ms` / `67 MiB` artifact above only measured `side_by_side_plan` plus a light run scan and is superseded as the current non-parser rebuild baseline.
- Rebaseline completion note (2026-04-06): benchmark credibility is no longer the gating issue. Local provenance work is exhausted, the incident-path syntax proxies now actually use JSON grammar plus JSON-shaped documents, and a fresh `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` still reports only the same two syntax-path timing alerts. The remaining missing incident facts stay documented as external follow-ups rather than as local measurement blockers.
- Primary goal: reduce the crash to one credible benchmark or one narrowly scoped harness reproduction before normal optimization work starts.
- Primary suspects: large-file syntax preparation, diff row materialization, word-diff/token work, or follow-on search/highlight passes triggered by a very large JSON payload.

## Rebaseline Checklist

- [x] Fresh run reset. Local repo/session/journal/history evidence is exhausted and that blocker is now documented in the incident notes above; the exact UI action still depends on an external incident note or user recollection.
- [x] Capture the input shape. Approximate size and the likely syntax path are recorded locally; the missing line count, minified-vs-pretty formatting, and exact syntax mode remain documented as external follow-up from the same missing incident note or user recollection.
- [x] Capture the failure mode. Local panic/OOM/coredump/journal evidence is recorded above; the current local summary rules out a straightforward Rust panic and leaves the final exit distinction documented as external follow-up from the same missing incident note or user recollection.
- [x] Run the existing large-text proxy commands from `perf.md` and record the first command that reproduces a crash, timeout, or suspicious allocation spike.
- [x] If existing proxy benchmarks do not reproduce the failure credibly, add a narrow JSON-specific perf fixture and benchmark case before doing optimization work.
- [x] Confirm which stage dominates the failure: syntax preparation, diff plan materialization, inline or split row paging, word-diff/token work, search/highlight follow-on work, or something outside the current suite.
- [x] After one reproduction is credible, record the first stable timing outliers and allocation outliers below. Do not start broad optimization passes before that point.

## Candidate Existing Benchmarks

Start with these existing cases before introducing new harness code:

- `file_diff_syntax_prepare/file_diff_syntax_prepare_cold`
- `file_diff_syntax_query_stress/nested_long_lines_cold`
- `diff_open_file_inline_first_window/200`
- `diff_open_file_split_first_window/200`

Use the size overrides from `perf.md` so the proxies are closer to the original 150 MB incident.

## Notes For The First Optimize Pass

Once a credible reproduction exists, the first optimize task should stay narrow:

- prefer reducing repeated full-document syntax work before touching UI polish or layout paths
- keep the same benchmark label and the same environment variables for before and after measurements
- if a proxy benchmark is not representative enough, replace it with a JSON-specific benchmark instead of optimizing against the wrong workload

## Recorded Timing Outliers

- `file_diff_syntax_query_stress/nested_long_lines_cold` with the corrected JSON `3,072 x 49,152-byte`, `192`-nesting proxy input: Criterion mean `2.0269 s` with 95% CI `2.0236 s` to `2.0300 s`.
- `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` with the corrected JSON `12,200 x 12,288-byte` proxy input: Criterion mean `1.5167 s` with 95% CI `1.5159 s` to `1.5175 s`.

## Recorded Allocation Outliers

- `file_diff_syntax_query_stress/nested_long_lines_cold` now has the largest confirmed native parser allocation footprint in the corrected JSON large-file path: `tree_sitter_alloc_bytes=643,268,552`, `tree_sitter_dealloc_bytes=388,440,200`, `tree_sitter_net_alloc_bytes=254,828,352`, `tree_sitter_alloc_ops=4,165,639`, and `tree_sitter_realloc_ops=1`.
- `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` is the second confirmed native parser allocation outlier after the same JSON-fixture correction: `tree_sitter_alloc_bytes=66,563,480`, `tree_sitter_dealloc_bytes=152`, `tree_sitter_net_alloc_bytes=66,563,328`, `tree_sitter_alloc_ops=658,807`, and `tree_sitter_realloc_ops=1`.
- `diff_refresh_rev_only_same_content/rebuild` remains the largest tracked Rust-side `PerfTrackingAllocator` footprint among the measured incident-path artifacts after the Turn 28 precomputed-line reuse follow-up: `alloc_bytes=160,478,464`, `alloc_ops=21`, `realloc_ops=3`, `net_alloc_bytes=0`, `rebuild_rows=1,200,000`, `rebuild_inline_rows=2,399,997`, `old_text_bytes=41,149,209`, `new_text_bytes=41,149,209`, `old_line_starts=1,200,001`, and `new_line_starts=1,200,001`. The older `189,187,328`-byte / `realloc_ops=41` sidecar from Turn 27, the Turn 26 `189,187,368` / `alloc_ops=23` sidecar, the Turn 25 `271,485,786`-byte sidecar, and the even older `67,108,960`-byte sidecar that only covered `side_by_side_plan` plus a light run scan are all superseded now.
- The corrected cold JSON syntax proxies still no longer show large Rust-side `PerfTrackingAllocator` spikes once setup is excluded from the measured closure: `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` reports `alloc_bytes=17,456`, `alloc_ops=6`, `net_alloc_bytes=1,624`, and `file_diff_syntax_query_stress/nested_long_lines_cold` reports `alloc_bytes=96,744`, `alloc_ops=8`, `net_alloc_bytes=12,632`. Treat the earlier ~`600 MiB` unprefixed syntax sidecars and the older Rust-grammar proxy sidecars as superseded harness artifacts rather than as current parser-path allocation baselines.

## First Confirmed Optimization Candidates

- [x] `file_diff_syntax_prepare/file_diff_syntax_prepare_cold`: retained an `8 MiB` prepared full-document tree-sitter byte gate so extreme multi-megabyte buffers stay on the existing visible-line heuristic fallback instead of building a whole-document parse tree. Under the same exact `12,200 x 12,288-byte` JSON override, the benchmark now runs at mean `22.760 us` with 95% CI `22.493 us` to `23.025 us`; sidecar `alloc_bytes=15,832`, `alloc_ops=4`, `realloc_ops=3`, `net_alloc_bytes=0`. No `tree_sitter_*` fields were emitted because the full-document parser path no longer ran for this workload.
- [x] `file_diff_syntax_query_stress/nested_long_lines_cold`: the same retained `8 MiB` gate also retired the sibling `3,072 x 49,152-byte`, `192`-nesting JSON guardrail under the same exact harness. The benchmark now runs at mean `59.704 us` with 95% CI `59.415 us` to `59.965 us`; sidecar `alloc_bytes=84,112`, `alloc_ops=4`, `realloc_ops=6`, `net_alloc_bytes=0`. No `tree_sitter_*` fields were emitted here either because the full-document parser path was skipped.
- [x] `diff_refresh_rev_only_same_content/rebuild`: retained the first post-rebaseline non-parser rebuild pass by removing the intermediate owned `String` copy in `build_file_diff_document_source(...)` and constructing `SharedString` directly from the borrowed diff text. Under the same exact `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000` override, the benchmark now runs at mean `74.086 ms` with 95% CI `74.032 ms` to `74.136 ms`; sidecar `alloc_bytes=189,187,368`, `alloc_ops=23`, `realloc_ops=41`, `net_alloc_bytes=0`, `full_rebuilds=1`, `rebuild_rows=1,200,000`, `rebuild_inline_rows=2,399,997`, `old_text_bytes=41,149,209`, `new_text_bytes=41,149,209`, `old_line_starts=1,200,001`, and `new_line_starts=1,200,001`. Against the corrected Turn 25 baseline (`95.926 ms`, `alloc_bytes=271,485,786`), that is about a `22.8%` timing improvement and a `30.3%` Rust-side allocation reduction, but the benchmark still remains above the `50 ms` budget.
- Retained-change note (2026-04-07): `StreamedFileDiffSource` now defers split and inline run-start offset vectors until the first row-oriented query instead of building both vectors during every rebuild. Under the same exact `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000` override, `diff_refresh_rev_only_same_content/rebuild` now reruns at mean `71.787 ms` with 95% CI `71.735 ms` to `71.834 ms`; sidecar `alloc_bytes=189,187,328`, `alloc_ops=21`, `realloc_ops=41`, `net_alloc_bytes=0`, `full_rebuilds=1`, `rebuild_rows=1,200,000`, `rebuild_inline_rows=2,399,997`, `old_text_bytes=41,149,209`, `new_text_bytes=41,149,209`, `old_line_starts=1,200,001`, and `new_line_starts=1,200,001`. Against the retained Turn 26 baseline (`74.086 ms`, `alloc_bytes=189,187,368`, `alloc_ops=23`), that is another `3.1%` timing improvement with effectively flat allocation bytes and two fewer Rust-side allocation ops, but the benchmark still remains above the `50 ms` budget.
- Retained-change note (2026-04-07): the file-diff rebuild path now derives exact-capacity `str::lines()` slices from the already-built `line_starts` arrays and feeds those precomputed slices into `side_by_side_plan_from_lines(...)`, which removes the extra whole-buffer `text.lines().collect()` scan and most `Vec<&str>` growth churn from `diff_refresh_rev_only_same_content/rebuild`. Under the same exact `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000` override, the benchmark now reruns at mean `46.700 ms` with 95% CI `46.588 ms` to `46.834 ms`; sidecar `alloc_bytes=160,478,464`, `alloc_ops=21`, `realloc_ops=3`, `net_alloc_bytes=0`, `full_rebuilds=1`, `rebuild_rows=1,200,000`, `rebuild_inline_rows=2,399,997`, `old_text_bytes=41,149,209`, `new_text_bytes=41,149,209`, `old_line_starts=1,200,001`, and `new_line_starts=1,200,001`. Against the retained Turn 27 baseline (`71.787 ms`, `alloc_bytes=189,187,328`), that is another `34.9%` timing improvement, about `27.4 MiB` fewer Rust-side allocation bytes, and a `41 -> 3` drop in `realloc_ops`; `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports `diff_refresh_rev_only_same_content/rebuild` at `46.834 ms` upper bound and `OK within budget`.
- Retained-change note (2026-04-07): the current worktree now declines prepared whole-document tree-sitter parsing above `8 MiB` and relies on the existing per-line heuristic fallback for those extreme buffers. Against the last post-gating exact baselines, `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` improved by about `67,211x` (`1.5297 s` -> `22.760 us`) and `file_diff_syntax_query_stress/nested_long_lines_cold` improved by about `34,091x` (`2.0350 s` -> `59.704 us`), so the former large-JSON parser-path outliers are no longer current timing or native-allocation hot spots on this worktree.
- [x] Validate the retained `8 MiB` prepared full-document syntax gate on real preview/diff UI paths and decide whether the cutoff should stay global or become language/path specific. New GPUI coverage now exercises oversized `.json` preview and file-diff UI paths with valid `>8 MiB` JSON-array fixtures that stay below the old `4,000`-line gate. Both paths keep `worktree_preview_prepared_syntax_document()` / `file_diff_split_prepared_syntax_document(...)` absent while visible preview, split, and inline rows still render heuristic syntax highlights, so the current `8 MiB` cutoff stays global for now. Fresh exact guardrail reruns remain in the same post-gate regime: `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` at mean `21.962 us` with 95% CI `21.782 us` to `22.167 us` and sidecar `alloc_bytes=15,832`, `alloc_ops=4`, `net_alloc_bytes=0`; `file_diff_syntax_query_stress/nested_long_lines_cold` at mean `59.755 us` with 95% CI `59.323 us` to `60.316 us` and sidecar `alloc_bytes=84,112`, `alloc_ops=4`, `net_alloc_bytes=0`. Neither rerun emitted `tree_sitter_*` fields.
- Attempt note (2026-04-06): a large-document source-identity cache-key fast path plus disabled adjacent chunk prefetch was measured and then reverted. `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` reran at `1.3985 s` with sidecar `alloc_bytes=600,809,270`, `alloc_ops=12,246`, `net_alloc_bytes=150,293,184`; Criterion reported change `[−3.1119% −1.3270% +0.6083%]`, `p=0.19`, and "No change in performance detected." The related `file_diff_syntax_query_stress/nested_long_lines_cold` reran at `1.7102 s` with sidecar `alloc_bytes=604,327,387`, `alloc_ops=3,085`, `net_alloc_bytes=151,102,384`; Criterion reported change `[−2.4038% −1.2769% −0.2036%]`, `p=0.04`, but still "Change within noise threshold." That attempt was discarded and the large-JSON syntax candidates stayed open until a different approach landed.
- Attempt note (2026-04-06): switching `treesitter_text_hash(...)` from `FxHasher` to `xxh3` for the prepared-syntax cache key was measured and then reverted. `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` reran at `1.5244 s` with 95% CI `1.5184 s` to `1.5314 s`; Criterion reported change `[−0.8074% −0.3515% +0.0764%]`, `p=0.17`, and "No change in performance detected." The sidecar stayed aligned with the post-gating JSON baseline at Rust-side `alloc_bytes=15,920`, `alloc_ops=5`, `net_alloc_bytes=88`, and native `tree_sitter_alloc_bytes=66,563,480`, `tree_sitter_alloc_ops=658,807`, `tree_sitter_net_alloc_bytes=66,563,328`. The sibling query-stress guardrail was not rerun because the candidate benchmark showed no retained signal and the code was reverted immediately.
- Harness reset note (2026-04-06): before the retained `8 MiB` gate above, the corrected JSON parser-path baselines for these two cold syntax candidates were `1.5167 s` / `alloc_bytes=17,456` / `tree_sitter_alloc_bytes=66,563,480` and `2.0269 s` / `alloc_bytes=96,744` / `tree_sitter_alloc_bytes=643,268,552`. The older ~`600 MiB` unprefixed sidecars in the attempt note above were collected before the cold-source setup fix, and the older `1.3406 s` / `1.6344 s` syntax timings were still Rust-grammar proxy runs before the Turn 18 JSON-fixture correction, so neither set is comparable as the final parser-path baseline that the retained gate retired.

## Next Benchmark Tasks

- Current-worktree note (2026-04-07): after the retained `8 MiB` syntax gate, its real-path UI validation, the Turn 26 document-source copy reduction, the Turn 27 lazy run-start follow-up, and the Turn 28 precomputed-line reuse pass, the former large-JSON parser proxies remain microsecond guardrails and `diff_refresh_rev_only_same_content/rebuild` is no longer an active timing alert. The latest exact rerun with `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000` is now mean `46.700 ms` with 95% CI `46.588 ms` to `46.834 ms`; the sidecar reports `alloc_bytes=160,478,464`, `alloc_ops=21`, `realloc_ops=3`, `net_alloc_bytes=0`, `full_rebuilds=1`, `rebuild_rows=1,200,000`, `rebuild_inline_rows=2,399,997`, `old_text_bytes=41,149,209`, `new_text_bytes=41,149,209`, `old_line_starts=1,200,001`, and `new_line_starts=1,200,001`. A fresh `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now says all non-skipped tracked view benchmarks are within budget.
- [x] `diff_refresh_rev_only_same_content/rebuild`: second post-rebaseline optimization target. Reused the already-built `line_starts` arrays to derive exact-capacity line slices for `side_by_side_plan_from_lines(...)` instead of rescanning the whole buffers with `text.lines().collect()`. Keep the same exact bench label and `GITCOMET_BENCH_DIFF_REFRESH_LINES=1200000` override for any future verification reruns; the current retained artifact is now `46.700 ms` mean / `46.834 ms` upper with sidecar `alloc_bytes=160,478,464`, `alloc_ops=21`, and `realloc_ops=3`.
- Current-worktree follow-up note (2026-04-07): there is no remaining active timing alert among the currently measured non-skipped view benchmarks. If another perf pass is needed, start by widening measurement coverage again rather than reopening the already-retained large-JSON parser path or the now-within-budget diff-refresh rebuild path.
- Broader-coverage note (2026-04-07): Turn 29 widened exact-run coverage with `history_cache_build/50k_commits_2k_refs_200_stashes` and found a benchmark-helper credibility bug before accepting the result. The shared fixture helper in `repo_history.rs` had drifted to a no-op stash analysis that left `visible_commits=50000`, `graph_rows=50000`, `commit_vms=50000`, and `stash_helpers_filtered=0`; that older `13.339 ms` / `alloc_bytes=26,120,330` artifact is now superseded as a harness error. After restoring the benchmark helper's stash-tip detection and helper filtering to match the production history-cache path, the same exact benchmark now lands at mean `14.702 ms` with 95% CI `14.419 ms` to `15.107 ms`; sidecar `alloc_bytes=29,572,354`, `alloc_ops=207,530`, `realloc_ops=1,750`, `net_alloc_bytes=0`, `visible_commits=49,800`, `graph_rows=49,800`, `commit_vms=49,800`, `stash_helpers_filtered=200`, `decorated_commits=1,879`, and `max_lanes=3`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports both the timing budget and the structural budgets for this benchmark as `OK within budget`.
- Broader-coverage note (2026-04-07): Turn 30 widened exact-run coverage again with `open_repo/history_heavy` and captured the first fresh open-repo history-skewed artifact on this worktree. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'open_repo/history_heavy' --exact` now lands at mean `1.115 ms` with 95% CI `1.111 ms` to `1.118 ms`; sidecar `alloc_bytes=4,027,799`, `alloc_ops=2,564`, `realloc_ops=27`, `net_alloc_bytes=0`, `commit_count=15,000`, `local_branches=100`, `remote_branches=400`, `remotes=2`, `worktrees=0`, `submodules=0`, `sidebar_rows=916`, `graph_rows=15,000`, and `max_graph_lanes=2`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 7,500 ms`) plus the structural `commit_count` and `graph_rows` checks for this label as `OK within budget`.
- Next-coverage note (2026-04-07): after `open_repo/history_heavy`, continue within the same shared group before jumping elsewhere. The next exact-run coverage target should be `open_repo/extreme_metadata_fanout`, which is still missing a fresh artifact and is the remaining open-repo variant most likely to expose sidebar-fanout cost that `history_heavy` does not stress.
- Broader-coverage note (2026-04-07): Turn 31 widened exact-run coverage with `open_repo/extreme_metadata_fanout` and surfaced a stale structural-budget assumption before accepting the result. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'open_repo/extreme_metadata_fanout' --exact` now lands at mean `1.900 ms` with 95% CI `1.896 ms` to `1.903 ms`; sidecar `alloc_bytes=3,280,591`, `alloc_ops=14,631`, `realloc_ops=21`, `net_alloc_bytes=0`, `commit_count=1,000`, `local_branches=1,000`, `remote_branches=10,000`, `remotes=1`, `worktrees=5,000`, `submodules=1,000`, `sidebar_rows=11,416`, `graph_rows=1,000`, and `max_graph_lanes=2`. The first report pass flagged `sidebar_rows` because the structural budget still assumed expanded worktree/submodule rows (`>= 17,000`), but the initial open-repo UI path keeps those sections collapsed by default and only renders their headers. Lowering the structural guardrail and its paired fixture assertion to `>= 11,400` restored measurement credibility, and a follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing budget (`<= 20.000 ms`) plus the structural `local_branches`, `remote_branches`, `worktrees`, `submodules`, and `sidebar_rows` checks as `OK within budget`.
- Next-coverage note (2026-04-07): after `open_repo/extreme_metadata_fanout`, stay in the same shared group and capture `open_repo/branch_heavy` next. It is still missing a fresh exact artifact on this worktree and remains the open-repo variant most likely to expose heavier sidebar branch fanout before falling back to the lighter `open_repo/balanced` case.
- Broader-coverage note (2026-04-07): Turn 32 widened exact-run coverage again with `open_repo/branch_heavy` and found that the current benchmark plus budget wiring are already credible. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'open_repo/branch_heavy' --exact` now lands at mean `707.516 us` with 95% CI `706.320 us` to `709.153 us`; sidecar `alloc_bytes=2,106,256`, `alloc_ops=8,162`, `realloc_ops=101`, `net_alloc_bytes=0`, `commit_count=1,000`, `local_branches=1,200`, `remote_branches=3,200`, `remotes=8`, `worktrees=0`, `submodules=0`, `sidebar_rows=5,030`, `graph_rows=1,000`, and `max_graph_lanes=2`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 30.000 ms`) plus the structural `local_branches`, `remote_branches`, and `sidebar_rows` checks as `OK within budget`, so no harness or budget correction was needed for this label.
- Next-coverage note (2026-04-07): after `open_repo/branch_heavy`, finish the shared open-repo group with `open_repo/balanced` before jumping elsewhere. `target/criterion/open_repo/` now has fresh exact artifacts for `history_heavy`, `extreme_metadata_fanout`, and `branch_heavy`, while `open_repo/balanced` is still missing a current exact-run artifact on this worktree.
- Broader-coverage note (2026-04-07): Turn 33 finished the shared open-repo group with `open_repo/balanced`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'open_repo/balanced' --exact` now lands at mean `488.203 us` with 95% CI `486.983 us` to `489.605 us`; sidecar `alloc_bytes=1,741,546`, `alloc_ops=3,074`, `realloc_ops=29`, `net_alloc_bytes=0`, `commit_count=5,000`, `local_branches=200`, `remote_branches=800`, `remotes=2`, `worktrees=0`, `submodules=0`, `sidebar_rows=1,418`, `graph_rows=5,000`, and `max_graph_lanes=2`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 650.000 ms`) plus the structural `commit_count` and `graph_rows` checks as `OK within budget`, so no harness or budget correction was needed for this label either.
- Next-coverage note (2026-04-07): with `open_repo/balanced` captured, the shared open-repo group now has fresh exact artifacts for `balanced`, `history_heavy`, `branch_heavy`, and `extreme_metadata_fanout`. The next exact-run coverage target should be `history_cache_build/balanced`, which is still missing a fresh artifact on this worktree and is the next unmeasured foundation-group baseline before the heavier `merge_dense`, `decorated_refs_heavy`, and `stash_heavy` variants.
- Broader-coverage note (2026-04-07): Turn 34 widened exact-run coverage into the shared `history_cache_build` group with `history_cache_build/balanced`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_cache_build/balanced' --exact` now lands at mean `1.248 ms` with 95% CI `1.227 ms` to `1.287 ms`; sidecar `alloc_bytes=3,027,130`, `alloc_ops=24,136`, `realloc_ops=1,400`, `net_alloc_bytes=0`, `visible_commits=5,000`, `graph_rows=5,000`, `commit_vms=5,000`, `stash_helpers_filtered=0`, `decorated_commits=997`, and `max_lanes=3`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 1,200.000 ms`) plus the structural `visible_commits >= 4900` and `graph_rows >= 4900` checks as `OK within budget`, so no harness or budget correction was needed for this label.
- Next-coverage note (2026-04-07): with `history_cache_build/balanced` captured, keep widening coverage within the same shared group and take `history_cache_build/merge_dense` next. It is the next missing foundation-group artifact on this worktree and is the most likely of the remaining history-cache variants to expose graph-density cost before the heavier ref-decoration and stash-heavy cases.
- Broader-coverage note (2026-04-07): Turn 35 widened exact-run coverage again with `history_cache_build/merge_dense`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_cache_build/merge_dense' --exact` now lands at mean `1.059 ms` with 95% CI `1.040 ms` to `1.079 ms`; sidecar `alloc_bytes=2,468,103`, `alloc_ops=20,183`, `realloc_ops=35`, `net_alloc_bytes=0`, `visible_commits=5,000`, `graph_rows=5,000`, `commit_vms=5,000`, `stash_helpers_filtered=0`, `decorated_commits=37`, and `max_lanes=3`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 1,200.000 ms`) as `OK within budget`. Despite the denser graph shape, this variant is slightly faster and lighter than the fresh `history_cache_build/balanced` baseline on the current worktree, so it is not the next timing or allocation outlier. There is still no dedicated structural budget row for `history_cache_build/merge_dense`; current credibility rests on the fresh sidecar shape plus the existing fixture assertion that the dense graph keeps `max_lanes > 1`.
- Next-coverage note (2026-04-07): with `history_cache_build/merge_dense` captured, stay in the same shared group and take `history_cache_build/decorated_refs_heavy` next. It is still missing a fresh exact artifact on this worktree and is now the remaining history-cache variant most likely to expose ref-decoration cost or a structural-budget mismatch before the stash-heavy case.
- Broader-coverage note (2026-04-07): Turn 36 widened exact-run coverage again with `history_cache_build/decorated_refs_heavy`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_cache_build/decorated_refs_heavy' --exact` now lands at mean `2.356 ms` with 95% CI `2.348 ms` to `2.368 ms`; sidecar `alloc_bytes=6,110,130`, `alloc_ops=47,239`, `realloc_ops=7,000`, `net_alloc_bytes=0`, `visible_commits=5,000`, `graph_rows=5,000`, `commit_vms=5,000`, `stash_helpers_filtered=0`, `decorated_commits=4,632`, and `max_lanes=3`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports both the timing budget (`<= 1,200.000 ms`) and the structural `decorated_commits >= 500` check as `OK within budget`. This is still the slowest and heaviest of the fresh 5k-commit history-cache variants on the current worktree, so it becomes the current local ceiling for that shared group without opening a new blocker.
- Next-coverage note (2026-04-07): with `history_cache_build/decorated_refs_heavy` captured, stay in the same shared group and take `history_cache_build/stash_heavy` next. It is the last missing fresh exact artifact in the 5k-commit history-cache set and the remaining variant most likely to expose stash-helper filtering cost or a structural-budget mismatch before widening coverage elsewhere.
- Broader-coverage note (2026-04-07): Turn 37 finished the shared `history_cache_build` 5k-commit group with `history_cache_build/stash_heavy`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_cache_build/stash_heavy' --exact` now lands at mean `1.196 ms` with 95% CI `1.194 ms` to `1.198 ms`; sidecar `alloc_bytes=2,907,023`, `alloc_ops=20,219`, `realloc_ops=175`, `net_alloc_bytes=0`, `visible_commits=4,800`, `graph_rows=4,800`, `commit_vms=4,800`, `stash_helpers_filtered=200`, `decorated_commits=145`, and `max_lanes=3`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 1,000.000 ms`) plus the structural `stash_helpers_filtered >= 100` check as `OK within budget`. Despite exercising stash-helper filtering, this variant is still faster and lighter than `history_cache_build/decorated_refs_heavy`, so it does not become the next local timing or allocation outlier.
- Next-coverage note (2026-04-07): with `history_cache_build/stash_heavy` captured, the shared 5k-commit history-cache group now has fresh exact artifacts for `balanced`, `merge_dense`, `decorated_refs_heavy`, and `stash_heavy`. The next exact-run coverage target should be `branch_sidebar/cache_hit_balanced`, which is still missing a fresh artifact on this worktree; `target/criterion/branch_sidebar/` currently only has `local_heavy`, `remote_fanout`, `aux_lists_heavy`, and `20k_branches_100_remotes`, while the branch-sidebar cache subgroup already has timing and structural budgets but no current exact artifact yet.
- Broader-coverage note (2026-04-07): Turn 38 widened exact-run coverage into the branch-sidebar cache subgroup with `branch_sidebar/cache_hit_balanced`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'branch_sidebar/cache_hit_balanced' --exact` now lands at mean `3.142 ns` with 95% CI `3.141 ns` to `3.144 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `cache_hits=1`, `cache_misses=0`, and `invalidations=0`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 1.000 us`) plus the structural `cache_misses == 0` check as `OK within budget`. No harness or budget correction was needed, and this confirms the cached branch-sidebar fast path still stays in nanosecond/no-allocation territory on the current worktree.
- Next-coverage note (2026-04-07): after `branch_sidebar/cache_hit_balanced`, stay in the same cache subgroup and capture `branch_sidebar/cache_miss_remote_fanout` next. It is still missing a fresh exact artifact on this worktree and is the heaviest remaining branch-sidebar cache case before the two invalidation variants.
- Broader-coverage note (2026-04-07): Turn 39 widened exact-run coverage again with `branch_sidebar/cache_miss_remote_fanout`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'branch_sidebar/cache_miss_remote_fanout' --exact` now lands at mean `758.529 us` with 95% CI `757.823 us` to `759.079 us`; sidecar `alloc_bytes=2,454,961`, `alloc_ops=9,153`, `realloc_ops=149`, `net_alloc_bytes=0`, `cache_hits=0`, `cache_misses=1`, `invalidations=1`, and `rows_count=5,586`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 50.000 ms`) plus the structural `cache_hits == 0` check as `OK within budget`. No harness or budget correction was needed, and this gives the current worktree its first fresh allocation-bearing branch-sidebar cache miss baseline.
- Next-coverage note (2026-04-07): after `branch_sidebar/cache_miss_remote_fanout`, stay in the same cache subgroup and capture `branch_sidebar/cache_invalidation_single_ref_change` next. It is still missing a fresh exact artifact on this worktree and is the remaining branch-sidebar cache invalidation path to baseline before the heavier `cache_invalidation_worktrees_ready` variant.
- Broader-coverage note (2026-04-07): Turn 40 widened exact-run coverage again with `branch_sidebar/cache_invalidation_single_ref_change`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'branch_sidebar/cache_invalidation_single_ref_change' --exact` now lands at mean `179.147 us` with 95% CI `178.860 us` to `179.441 us`; sidecar `alloc_bytes=628,650`, `alloc_ops=3,070`, `realloc_ops=29`, `net_alloc_bytes=0`, `cache_hits=0`, `cache_misses=1`, `invalidations=1`, and `rows_count=1,418`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` reports the timing budget (`<= 10.000 ms`) plus the structural `cache_hits == 0` check as `OK within budget`. No harness or budget correction was needed, and this lighter invalidation path stays about `4.23x` faster and `3.91x` lighter in `alloc_bytes` than the fresh `branch_sidebar/cache_miss_remote_fanout` baseline, so it does not become the cache-subgroup ceiling.
- Next-coverage note (2026-04-07): after `branch_sidebar/cache_invalidation_single_ref_change`, stay in the same cache subgroup and capture `branch_sidebar/cache_invalidation_worktrees_ready` next. It is still missing a fresh exact artifact on this worktree and is the last branch-sidebar cache path with both timing and structural budgets before widening coverage elsewhere.
- Branch-sidebar cache blocker note (2026-04-07): Turn 41 targeted `branch_sidebar/cache_invalidation_worktrees_ready` and added cache-row breakdown sidecar metrics (`worktree_rows`, `submodule_rows`, `stash_rows`) so the cache subgroup can prove whether aux-list rows participate in representative runs. The fresh exact rerun lands at mean `164.764 us` with 95% CI `164.135 us` to `165.613 us`; sidecar `alloc_bytes=628,650`, `alloc_ops=3,070`, `realloc_ops=29`, `net_alloc_bytes=0`, `cache_hits=0`, `cache_misses=1`, `invalidations=1`, and `rows_count=1,418`, but all three aux-row metrics stayed at `0`. A follow-up exact rerun of `branch_sidebar/cache_hit_balanced` also reports `rows_count=1,418` with zero worktree/submodule/stash rows, and an explicit control probe with `GITCOMET_BENCH_WORKTREES=2`, `GITCOMET_BENCH_SUBMODULES=3`, and `GITCOMET_BENCH_STASHES=4` still stayed at zero. The cache benchmark mirror is therefore not carrying aux-list rows into its cached row slice, so `branch_sidebar/cache_invalidation_worktrees_ready` remains a measurement blocker rather than a credible full-shape baseline.
- Next-coverage note (2026-04-07): before widening coverage elsewhere, fix the branch-sidebar cache benchmark mirror in `crates/gitcomet-ui-gpui/src/view/rows/benchmarks/repo_history.rs` so the cached fixture actually exercises worktree/submodule/stash rows and reflects the real branch-sidebar cache/source-fingerprint behavior from `crates/gitcomet-ui-gpui/src/view/branch_sidebar.rs`. Then rerun `branch_sidebar/cache_hit_balanced` and `branch_sidebar/cache_invalidation_worktrees_ready` on the default env to capture credible artifacts for the cache subgroup.
- Broader-coverage note (2026-04-07): Turn 42 resolved that branch-sidebar cache blocker. The benchmark mirror now uses the production cache/source-fingerprint helpers from `crates/gitcomet-ui-gpui/src/view/{branch_sidebar,caches}.rs`, explicitly expands the default-collapsed worktree/submodule/stash sections inside the cache fixture, and mutates real branch/remote/worktree source snapshots on the miss labels so the cache subgroup no longer relies on rev-only false misses. Fresh exact artifacts on the default env are now: `branch_sidebar/cache_hit_balanced` mean `3.617 ns` with sidecar `alloc_bytes=0`, `cache_hits=1`, `rows_count=1,948`, `worktree_rows=80`, `submodule_rows=150`, `stash_rows=300`; `branch_sidebar/cache_miss_remote_fanout` mean `950.749 us` with sidecar `alloc_bytes=2,895,763`, `alloc_ops=18,758`, `realloc_ops=154`, `net_alloc_bytes=22`, and `rows_count=5,589`; `branch_sidebar/cache_invalidation_single_ref_change` mean `178.878 us` with sidecar `alloc_bytes=655,245`, `alloc_ops=3,277`, `realloc_ops=32`, `rows_count=1,421`; and `branch_sidebar/cache_invalidation_worktrees_ready` mean `186.035 us` with sidecar `alloc_bytes=769,293`, `alloc_ops=3,545`, `realloc_ops=32`, `net_alloc_bytes=-12`, `rows_count=1,948`, `worktree_rows=80`, `submodule_rows=150`, and `stash_rows=300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports all four timing budgets plus the new aux-row structural checks as `OK within budget`, so the older `rows_count=1,418` / zero-aux artifacts for `cache_hit_balanced` and `cache_invalidation_worktrees_ready` are superseded as default-collapse mirror errors.
- Next-coverage note (2026-04-07): with the cache subgroup credible again, stay in the broader branch-sidebar area and capture a fresh exact artifact for `branch_sidebar/aux_lists_heavy` next. It is the remaining branch-sidebar label most directly tied to the aux-list-heavy row shape and currently only has an older non-exact artifact on this worktree.
- Broader-coverage note (2026-04-07): Turn 43 widened exact-run coverage again with `branch_sidebar/aux_lists_heavy`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'branch_sidebar/aux_lists_heavy' --exact` now lands at mean `162.180 us` with 95% CI `161.674 us` to `162.520 us`; sidecar `alloc_bytes=529,870`, `alloc_ops=3,321`, `realloc_ops=29`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/branch_sidebar/aux_lists_heavy/new/estimates.json` and `sidecar.json` on this worktree. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` still shows no timing or structural budget row for this label, so the fresh Criterion artifact itself is the current credibility record. No harness or budget correction was needed.
- Next-coverage note (2026-04-07): after `branch_sidebar/aux_lists_heavy`, stay in the broader branch-sidebar area and capture `branch_sidebar/20k_branches_100_remotes` next. It is still a tracked `perf_budget_report` label with `SKIP skipped (not run)` on this worktree and is the remaining branch-sidebar case most likely to surface a stale or missing exact artifact before widening elsewhere.
- Broader-coverage note (2026-04-07): Turn 44 widened exact-run coverage again with `branch_sidebar/20k_branches_100_remotes`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'branch_sidebar/20k_branches_100_remotes' --exact` now lands at mean `2.013 ms` with 95% CI `2.007 ms` to `2.018 ms`; sidecar `alloc_bytes=3,396,762`, `alloc_ops=22,433`, `realloc_ops=991`, `net_alloc_bytes=0`, `local_branches=1`, `remote_branches=20,000`, `remotes=100`, `branch_rows=20,002`, `remote_headers=100`, `group_headers=300`, `max_branch_depth=4`, and `sidebar_rows=20,414`. The exact run refreshed both `target/criterion/branch_sidebar/20k_branches_100_remotes/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 02:31:19 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing budget (`<= 250.000 ms`) plus all eight structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. This is now the slowest and heaviest cold branch-sidebar build captured on the current worktree, but it still does not displace the existing broader timing or allocation outlier set.
- Next-coverage note (2026-04-07): with the broader branch-sidebar group now refreshed through `branch_sidebar/20k_branches_100_remotes`, widen exact-run coverage into `history_load_more_append/page_500` next. It is the next tracked label with both timing and structural budgets still reporting `SKIP skipped (not run)` on this worktree, and its exact artifact is still missing under `target/criterion/`.
- Broader-coverage note (2026-04-07): Turn 45 widened exact-run coverage into `history_load_more_append/page_500`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_load_more_append/page_500' --exact` now lands at mean `894.083 ns` with 95% CI `891.775 ns` to `896.288 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=1`, `dealloc_bytes=52,000`, `realloc_ops=0`, `net_alloc_bytes=-52,000`, `existing_commits=5,000`, `appended_commits=500`, `total_commits_after_append=5,500`, `next_cursor_present=1`, `follow_up_effect_count=0`, `log_rev_delta=2`, and `log_loading_more_cleared=1`. The exact run refreshed both `target/criterion/history_load_more_append/page_500/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 02:33:59 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing budget (`<= 250.000 us`) plus all six structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. No harness or budget correction was needed; the zero-allocation, negative-net sidecar is consistent with the pre-seeded reducer append path reusing history capacity and only freeing one consumed page buffer, so this label does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `history_load_more_append/page_500`, stay in the adjacent history-state coverage and capture `history_scope_switch/current_branch_to_all_refs` next. It is the next tracked label in `perf_budget_report` still reporting `SKIP skipped (not run)` on this worktree, and its timing plus structural budgets are already wired.
- Broader-coverage note (2026-04-07): Turn 46 widened exact-run coverage again with `history_scope_switch/current_branch_to_all_refs`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_scope_switch/current_branch_to_all_refs' --exact` now lands at mean `3.377 us` with 95% CI `3.344 us` to `3.414 us`; sidecar `alloc_bytes=407`, `alloc_ops=3`, `dealloc_ops=3`, `realloc_ops=1`, `realloc_bytes_delta=-182`, `net_alloc_bytes=0`, `existing_commits=5,000`, `scope_changed=1`, `log_rev_delta=2`, `log_set_to_loading=1`, `load_log_effect_count=1`, and `persist_session_effect_count=0`. The exact run refreshed `target/criterion/history_scope_switch/current_branch_to_all_refs/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 02:36:15 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing budget (`<= 500.000 us`) plus all five structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. No harness or budget correction was needed, and this scope-switch reducer stays in low-microsecond / near-zero-allocation territory, so it does not enter the current outlier set.
- Next-coverage note (2026-04-07): after `history_scope_switch/current_branch_to_all_refs`, widen exact-run coverage into `repo_switch/refocus_same_repo` next. It is the first tracked `repo_switch` label still reporting `SKIP skipped (not run)` on this worktree, `target/criterion/repo_switch/` is still empty, and its benchmark plus timing/structural budget wiring are already present in `crates/gitcomet-ui-gpui/benches/performance/repo_switch.rs` and `perf_budget_report`.
- Broader-coverage note (2026-04-07): Turn 47 widened exact-run coverage into `repo_switch/refocus_same_repo`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/refocus_same_repo' --exact` now lands at mean `115.107 ns` with 95% CI `114.806 ns` to `115.519 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=5`, `refresh_effect_count=4`, `selected_diff_reload_effect_count=0`, `persist_session_effect_count=0`, `repo_count=1`, `hydrated_repo_count=1`, `selected_commit_repo_count=1`, and `selected_diff_repo_count=1`. The exact run refreshed `target/criterion/repo_switch/refocus_same_repo/new/estimates.json` and `sidecar.json` on this worktree. The first follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` exposed a stale structural budget expecting `effect_count == 6`, but the fixture test in `crates/gitcomet-ui-gpui/src/view/rows/benchmarks/tests.rs` already asserts `effect_count == 5` for this same-repo refocus path. Lowering the structural threshold to `5` in `crates/gitcomet-ui-gpui/src/bin/perf_budget_report/budgets/structural/history_status.rs` restored credibility, and a second `perf_budget_report -- --skip-missing` pass now reports the timing row plus both structural checks (`effect_count == 5`, `selected_diff_reload_effect_count == 0`) as `OK within budget`. This reducer remains deep in the sub-microsecond / zero-allocation regime, so it does not enter the current outlier set.
- Next-coverage note (2026-04-07): after `repo_switch/refocus_same_repo`, stay in the same `repo_switch` group and capture `repo_switch/two_hot_repos` next. It is the next tracked label in that group still reporting `SKIP skipped (not run)` on this worktree, and it is the first repo-switch variant with both hot-repo persistence and selected-diff reload effects. Keep an eye on the repo-switch logical counters while doing that rerun: `refocus_same_repo` still reports `refresh_effect_count=4` even though the fixture tests historically expected `6`, so `two_hot_repos` is the right place to confirm whether the current sidecar semantics or the older expectations need the next narrow correction.
- Broader-coverage note (2026-04-07): Turn 48 widened exact-run coverage into `repo_switch/two_hot_repos` and corrected the repo-switch harness semantics while doing so. `RepoSwitchFixture::fresh_state()` now restamps hydrated repos as hot by setting `last_active_at` on clone, and `RepoSwitchMetrics` now counts the current combined reducer effects (`LoadSelectedDiff`, `LoadSelectedConflictFile`, and `LoadRebaseAndMergeState`) instead of only the older split forms. The repo-switch structural budgets and benchmark expectations were updated to that combined-effect model at the same time. A fresh exact rerun of `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/two_hot_repos' --exact` now lands at mean `119.174 ns` with 95% CI `119.051 ns` to `119.322 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=2`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=2`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row plus all four structural checks for this label as `OK within budget`, so the earlier `208 ns` / `12 effects` artifact is superseded as a cold-restamp plus stale-counter harness error rather than a real baseline. A targeted `cargo test -p gitcomet-ui-gpui --features benchmarks repo_switch_two_hot_repos_reloads_selected_diff_and_persists_session -- --exact` still fails before reaching this test because unrelated benchmark-test drift elsewhere in `crates/gitcomet-ui-gpui/src/view/rows/benchmarks/tests.rs` references missing `RepoTabDragFixture::hit_test_steps`, `CommitSearchFilterFixture::distinct_message_trigrams`, `FileFuzzyFindFixture::run_find_without_ordered_pair_prefilter`, and absent parser helpers, so the bench/report pair remains the current verification path for this turn.
- Next-coverage note (2026-04-07): after `repo_switch/two_hot_repos`, stay in the same corrected `repo_switch` group and capture `repo_switch/selected_commit_and_details` next. It is the next tracked repo-switch label still reporting `SKIP skipped (not run)` on this worktree, its structural budgets now reflect the corrected hot-repo combined-effect model (`effect_count == 6`, `selected_diff_reload_effect_count == 0`, `persist_session_effect_count == 1`), and it is the right control case to confirm that the harness fix cleanly separates hot-repo refresh from selected-diff reload across the remaining repo-switch variants before widening elsewhere again.
- Broader-coverage note (2026-04-07): Turn 49 widened exact-run coverage into `repo_switch/selected_commit_and_details`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/selected_commit_and_details' --exact` now lands at mean `94.091 ns` with 95% CI `93.950 ns` to `94.201 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=6`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=0`, `persist_session_effect_count=1`, `repo_count=2`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=0`. The exact run refreshed `target/criterion/repo_switch/selected_commit_and_details/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:00:11 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus all five structural checks for this label as `OK within budget`, so no further harness or budget correction was needed. This zero-allocation control confirms the corrected hot-repo harness cleanly separates selected-commit/detail refresh from the selected-diff reload path, and it becomes the fastest fresh repo-switch reducer baseline on the current worktree.
- Next-coverage note (2026-04-07): after `repo_switch/selected_commit_and_details`, stay in the same corrected `repo_switch` group and capture `repo_switch/twenty_tabs` next. It is the next tracked repo-switch label still reporting `SKIP skipped (not run)` on this worktree, and it is the first scale case that should confirm the hot-restamp semantics still hold when repo count increases without heating every tab. If that remains clean, take `repo_switch/20_repos_all_hot` after it before widening elsewhere.
- Broader-coverage note (2026-04-07): Turn 50 widened exact-run coverage into `repo_switch/twenty_tabs`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/twenty_tabs' --exact` now lands at mean `127.398 ns` with 95% CI `126.958 ns` to `127.875 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=20`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=2`. The exact run refreshed `target/criterion/repo_switch/twenty_tabs/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:02:20 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 500.000 us`) plus all six structural checks for this label as `OK within budget`, so no further harness or budget correction was needed. This scale case stays allocation-free and only slightly slower than the fresh `repo_switch/two_hot_repos` baseline, which confirms the corrected hot-restamp path still scales cleanly when most tabs stay cold.
- Next-coverage note (2026-04-07): after `repo_switch/twenty_tabs`, stay in the same corrected `repo_switch` group and capture `repo_switch/20_repos_all_hot` next. It is the remaining repo-switch scale case still reporting `SKIP skipped (not run)` on this worktree and the right comparison point for how much the same reducer path grows when all twenty repos are hydrated hot instead of only the selected pair.
- Broader-coverage note (2026-04-07): Turn 51 widened exact-run coverage into `repo_switch/20_repos_all_hot`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/20_repos_all_hot' --exact` now lands at mean `128.034 ns` with 95% CI `127.586 ns` to `128.370 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=20`, `hydrated_repo_count=20`, `selected_commit_repo_count=20`, and `selected_diff_repo_count=20`. The exact run refreshed `target/criterion/repo_switch/20_repos_all_hot/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:04:29 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 2.000 ms`) plus all six structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This all-hot scale case stays allocation-free and is effectively flat versus `repo_switch/twenty_tabs` (`128.034 ns` vs `127.398 ns`), so the corrected repo-switch reducer still does not enter the current outlier set even when every tab remains hydrated.
- Next-coverage note (2026-04-07): after `repo_switch/20_repos_all_hot`, stay in the same corrected `repo_switch` group and capture `repo_switch/selected_diff_file` next. It is the next tracked repo-switch label still reporting `SKIP skipped (not run)` on this worktree and the first remaining variant that carries a loaded selected diff/file payload. If that remains clean, continue with `repo_switch/selected_conflict_target` and `repo_switch/merge_active_with_draft_restore` before widening into other still-missing `perf_budget_report` groups.
- Broader-coverage note (2026-04-07): Turn 52 widened exact-run coverage into `repo_switch/selected_diff_file`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/selected_diff_file' --exact` now lands at mean `122.097 ns` with 95% CI `121.938 ns` to `122.232 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=2`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=2`. The exact run refreshed `target/criterion/repo_switch/selected_diff_file/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:06:36 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 200.000 us`) plus all four structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This loaded-diff repo-switch case stays allocation-free and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `repo_switch/selected_diff_file`, stay in the same corrected `repo_switch` group and capture `repo_switch/selected_conflict_target` next. It is the next tracked repo-switch label still reporting `SKIP skipped (not run)` on this worktree and is the direct conflict-target sibling of the now-verified loaded-diff case. If that remains clean, continue with `repo_switch/merge_active_with_draft_restore` before widening into other still-missing `perf_budget_report` groups.
- Broader-coverage note (2026-04-07): Turn 53 widened exact-run coverage into `repo_switch/selected_conflict_target`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/selected_conflict_target' --exact` now lands at mean `148.843 ns` with 95% CI `148.750 ns` to `148.925 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=2`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=2`. The exact run refreshed `target/criterion/repo_switch/selected_conflict_target/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:08:36 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 200.000 us`) plus all four structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This conflict-target sibling stays allocation-free and remains in the same low-hundreds-of-nanoseconds regime as the other fresh repo-switch variants, so it does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `repo_switch/selected_conflict_target`, stay in the same corrected `repo_switch` group and capture `repo_switch/merge_active_with_draft_restore` next. It is the remaining tracked repo-switch label still reporting `SKIP skipped (not run)` on this worktree and the only repo-switch variant that adds a loaded draft merge message on top of the already-verified hot-repo switch path.
- Broader-coverage note (2026-04-07): Turn 54 finished the corrected `repo_switch` group with `repo_switch/merge_active_with_draft_restore`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'repo_switch/merge_active_with_draft_restore' --exact` now lands at mean `126.498 ns` with 95% CI `125.557 ns` to `127.104 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=0`, `effect_count=7`, `refresh_effect_count=5`, `selected_diff_reload_effect_count=1`, `persist_session_effect_count=1`, `repo_count=2`, `hydrated_repo_count=2`, `selected_commit_repo_count=2`, and `selected_diff_repo_count=2`. The exact run refreshed `target/criterion/repo_switch/merge_active_with_draft_restore/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:10:35 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 200.000 us`) plus all four structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This loaded-draft sibling also stays allocation-free and remains in the same low-hundreds-of-nanoseconds regime as the other fresh repo-switch variants, so it does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): with `repo_switch/merge_active_with_draft_restore` captured, the corrected repo-switch group now has fresh exact artifacts for every tracked `perf_budget_report` label on this worktree. Widen exact-run coverage into `status_list/unstaged_large` next; it is the first remaining tracked foundation-group label still reporting `SKIP skipped (not run)` after the repo-switch block and the right place to resume broader coverage without revisiting already-verified repo-switch paths.
- Broader-coverage note (2026-04-07): Turn 55 widened exact-run coverage into `status_list/unstaged_large`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_list/unstaged_large' --exact` now lands at mean `10.849 us` with 95% CI `10.491 us` to `11.157 us`; sidecar `alloc_bytes=9,600`, `alloc_ops=200`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=9,600`, `rows_requested=200`, `rows_painted=200`, `entries_total=10,000`, `path_display_cache_hits=0`, `path_display_cache_misses=200`, `path_display_cache_clears=0`, `max_path_depth=5`, and `prewarmed_entries=0`. The exact run refreshed `target/criterion/status_list/unstaged_large/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:12:26 +0300`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 250.000 us`) plus all four structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This cold-cache visible-window status-list path stays well below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `status_list/unstaged_large`, stay in the same `status_list` group and capture `status_list/staged_large` next. It is the adjacent tracked label still reporting `SKIP skipped (not run)` on this worktree and the right control case for whether the staged-file mix changes the same cold path-display cache behavior before widening into the heavier `status_list/20k_entries_mixed_depth` prewarm case.
- Broader-coverage note (2026-04-07): Turn 56 widened exact-run coverage into `status_list/staged_large`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_list/staged_large' --exact` now lands at mean `8.379 us` with 95% CI `8.369 us` to `8.390 us`; sidecar `alloc_bytes=12,800`, `alloc_ops=200`, `dealloc_ops=0`, `realloc_ops=0`, `net_alloc_bytes=12,800`, `rows_requested=200`, `rows_painted=200`, `entries_total=10,000`, `path_display_cache_hits=0`, `path_display_cache_misses=200`, `path_display_cache_clears=0`, `max_path_depth=5`, and `prewarmed_entries=0`. The exact run refreshed both `target/criterion/status_list/staged_large/new/estimates.json` and `sidecar.json` at `2026-04-07 03:14:21 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 250.000 us`) plus all four structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This staged-file sibling remains comfortably below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `status_list/staged_large`, stay in the same `status_list` group and capture `status_list/20k_entries_mixed_depth` next. It is the remaining tracked `status_list` label still reporting `SKIP skipped (not run)` on this worktree and the first prewarmed-cache case, so it is the right follow-up to confirm whether crossing the path-display cache clear threshold changes the same visible-window row-build path before widening elsewhere.
- Broader-coverage note (2026-04-07): Turn 57 widened exact-run coverage into `status_list/20k_entries_mixed_depth`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_list/20k_entries_mixed_depth' --exact` now lands at mean `7.388 us` with 95% CI `7.366 us` to `7.408 us`; sidecar `alloc_bytes=1,431,480`, `alloc_ops=8,393`, `dealloc_ops=201`, `realloc_ops=0`, `net_alloc_bytes=1,397,192`, `rows_requested=200`, `rows_painted=200`, `entries_total=20,000`, `path_display_cache_hits=0`, `path_display_cache_misses=200`, `path_display_cache_clears=0`, `max_path_depth=15`, and `prewarmed_entries=8,193`. The exact run refreshed both `target/criterion/status_list/20k_entries_mixed_depth/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:16:18 +0300`, replacing the older sidecar-only artifact from `2026-04-07 00:47:17 +0300` with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1.000 ms`) plus all seven structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. This prewarmed mixed-depth case still does not enter the current timing or allocation outlier set; the larger alloc sidecar reflects the cache prewarm inside the sidecar closure, while the path-display structural counters are reset after prewarm and still describe only the post-prewarm 200-row window.
- Next-coverage note (2026-04-07): with `status_list/20k_entries_mixed_depth` captured, the tracked `status_list` group now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `status_multi_select/range_select` next; it is the next tracked status/foundation label still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/status_multi_select/range_select/new/` currently only has a stale sidecar without a matching `estimates.json`.
- Broader-coverage note (2026-04-07): Turn 58 widened exact-run coverage into `status_multi_select/range_select`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_multi_select/range_select' --exact` now lands at mean `283.599 us` with 95% CI `278.331 us` to `286.358 us`; sidecar `alloc_bytes=28,308`, `alloc_ops=518`, `dealloc_ops=518`, `realloc_ops=0`, `net_alloc_bytes=0`, `entries_total=20,000`, `selected_paths=512`, `anchor_index=4,096`, `clicked_index=4,607`, `anchor_preserved=1`, and `position_scan_steps=0`. The exact run refreshed both `target/criterion/status_multi_select/range_select/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:19:08 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1.000 ms`) plus all six structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. This 512-path range-selection reducer stays well below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `status_multi_select/range_select`, stay in the adjacent status-interaction coverage and capture `status_select_diff_open/unstaged` next. It is the next tracked foundation label still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/status_select_diff_open/` is still empty. If that passes cleanly, take `status_select_diff_open/staged` before widening into a different still-missing `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 59 widened exact-run coverage into `status_select_diff_open/unstaged`, but the first fresh artifact exposed a structural-budget mismatch: the inline `SelectDiff` benchmark now returns one `LoadSelectedDiff` effect instead of the older eager `LoadDiff` plus `LoadDiffFile` pair. Turn 59 fixed that harness/report drift by adding `load_selected_diff_effect_count` to the benchmark sidecar, updating the status-select fixture expectations to the current inline reducer contract, and changing the structural budgets to require `load_selected_diff_effect_count == 1` while pinning `load_diff_effect_count == 0` and `load_diff_file_effect_count == 0`. After rerunning the exact bench, `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_select_diff_open/unstaged' --exact` now lands at mean `74.324 ns` with 95% CI `73.742 ns` to `74.753 ns`; sidecar `effect_count=1`, `load_selected_diff_effect_count=1`, `load_diff_effect_count=0`, `load_diff_file_effect_count=0`, `diff_state_rev_delta=1`, `alloc_bytes=31`, `alloc_ops=1`, and `net_alloc_bytes=31`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1.000 ms`) plus all five structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree and the earlier missing-metric / stale-budget report is superseded.
- Next-coverage note (2026-04-07): after `status_select_diff_open/unstaged`, stay in the same status-interaction group and capture `status_select_diff_open/staged` next. It is the adjacent tracked label still reporting `SKIP skipped (not run)` on this worktree, and it is now the right control case to confirm the corrected `load_selected_diff_effect_count` semantics hold for both staged and unstaged selection paths before widening into a different missing `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 60 widened exact-run coverage into `status_select_diff_open/staged`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'status_select_diff_open/staged' --exact` now lands at mean `76.929 ns` with 95% CI `76.733 ns` to `77.258 ns`; sidecar `effect_count=1`, `load_selected_diff_effect_count=1`, `load_diff_effect_count=0`, `load_diff_file_effect_count=0`, `load_diff_file_image_effect_count=0`, `diff_state_rev_delta=1`, `alloc_bytes=46`, `alloc_ops=1`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=46`. The exact run refreshed both `target/criterion/status_select_diff_open/staged/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:30:06 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 10.000 us`) plus all five structural checks for this label as `OK within budget`, so the tracked `status_select_diff_open` pair is now fully covered on this worktree. This staged sibling remains in the same low-nanosecond regime as `unstaged` and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): with `status_select_diff_open/staged` captured, the tracked `status_select_diff_open` pair now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `merge_open_bootstrap/small` next; it is the next tracked timing row still reporting `SKIP skipped (not run)` after the status-select block, and `target/criterion/merge_open_bootstrap/small/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`.
- Broader-coverage note (2026-04-07): Turn 61 widened exact-run coverage into `merge_open_bootstrap/small`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'merge_open_bootstrap/small' --exact` now lands at mean `54.800 us` with 95% CI `54.176 us` to `55.272 us`; sidecar `trace_event_count=7`, `conflict_block_count=0`, `three_way_visible_rows=5,000`, `rendering_mode_streamed=0`, `full_output_generated=1`, `full_syntax_parse_requested=1`, `bootstrap_total_ms=0.37783`, `alloc_bytes=18,766`, `alloc_ops=26`, `dealloc_ops=26`, `realloc_ops=43`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/merge_open_bootstrap/small/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:32:15 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1.000 ms`) as `OK within budget`; this label has no structural budget row wired today, and no harness or budget correction was needed. This small eager full-output bootstrap path stays well below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `merge_open_bootstrap/small`, stay in the same group and capture `merge_open_bootstrap/large_streamed` next. It is the adjacent tracked merge-bootstrap label still reporting `SKIP skipped (not run)` on the timing side of `perf_budget_report`, and a fresh exact run will verify that the already-present structural-budget rows still line up with a matched `estimates.json` plus `sidecar.json` artifact on this worktree. If that passes cleanly, continue with `merge_open_bootstrap/many_conflicts` before widening elsewhere.
- Broader-coverage note (2026-04-07): Turn 62 widened exact-run coverage into `merge_open_bootstrap/large_streamed`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'merge_open_bootstrap/large_streamed' --exact` now lands at mean `36.739 ms` with 95% CI `36.657 ms` to `36.832 ms`; sidecar `trace_event_count=7`, `conflict_block_count=1`, `diff_row_count=7`, `inline_row_count=0`, `resolved_output_line_count=55,001`, `two_way_visible_rows=7`, `three_way_visible_rows=55,001`, `rendering_mode_streamed=1`, `full_output_generated=0`, `full_syntax_parse_requested=1`, `whole_block_diff_ran=0`, `rss_kib=109,272`, `bootstrap_total_ms=39.272795`, `alloc_bytes=1,451,144`, `alloc_ops=55`, `dealloc_ops=55`, `realloc_ops=45`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/merge_open_bootstrap/large_streamed/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:33:57 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 ms`) plus all nine structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This streamed large-output merge bootstrap case stays well below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `merge_open_bootstrap/large_streamed`, stay in the same group and capture `merge_open_bootstrap/many_conflicts` next. It is the adjacent tracked merge-bootstrap label still reporting `SKIP skipped (not run)` on this worktree, and it is the right follow-up to confirm how the same streamed bootstrap path scales once conflict-block fanout becomes dense before taking `merge_open_bootstrap/50k_lines_500_conflicts_streamed`.
- Broader-coverage note (2026-04-07): Turn 63 widened exact-run coverage into `merge_open_bootstrap/many_conflicts`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'merge_open_bootstrap/many_conflicts' --exact` now lands at mean `352.533 us` with 95% CI `352.116 us` to `352.877 us`; sidecar `trace_event_count=7`, `conflict_block_count=50`, `diff_row_count=350`, `inline_row_count=0`, `resolved_output_line_count=600`, `two_way_visible_rows=350`, `three_way_visible_rows=600`, `rendering_mode_streamed=1`, `full_output_generated=0`, `full_syntax_parse_requested=1`, `whole_block_diff_ran=0`, `rss_kib=104,364`, `bootstrap_total_ms=0.447379`, `alloc_bytes=208,610`, `alloc_ops=303`, `dealloc_ops=303`, `realloc_ops=56`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/merge_open_bootstrap/many_conflicts/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:35:56 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 20.000 ms`) plus all six structural checks for this label as `OK within budget`, so the row is no longer `SKIP` on this worktree. This dense-conflict streamed bootstrap case remains well below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `merge_open_bootstrap/many_conflicts`, stay in the same group and capture `merge_open_bootstrap/50k_lines_500_conflicts_streamed` next. It is the remaining tracked merge-bootstrap timing row still reporting `SKIP skipped (not run)` on this worktree, and it is the right large-scale follow-up now that the smaller dense-conflict sibling has a fresh matched artifact pair.
- Broader-coverage note (2026-04-07): Turn 64 widened exact-run coverage into `merge_open_bootstrap/50k_lines_500_conflicts_streamed`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'merge_open_bootstrap/50k_lines_500_conflicts_streamed' --exact` now lands at mean `27.118 ms` with 95% CI `27.073 ms` to `27.175 ms`; sidecar `trace_event_count=7`, `conflict_block_count=500`, `diff_row_count=3,500`, `inline_row_count=0`, `resolved_output_line_count=50,000`, `two_way_visible_rows=3,500`, `three_way_visible_rows=50,000`, `rendering_mode_streamed=1`, `full_output_generated=0`, `full_syntax_parse_requested=1`, `whole_block_diff_ran=0`, `rss_kib=136,380`, `bootstrap_total_ms=28.878643`, `alloc_bytes=2,432,318`, `alloc_ops=2,555`, `dealloc_ops=2,555`, `realloc_ops=65`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/merge_open_bootstrap/50k_lines_500_conflicts_streamed/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:37:31 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 500.000 ms`) plus all seven structural checks for this label as `OK within budget`, so the tracked `merge_open_bootstrap` group now has fresh exact artifacts for every label on this worktree. This large-scale streamed bootstrap path remains well below budget; it is the heaviest fresh merge-bootstrap allocation baseline on this worktree, but it still does not enter the current timing or allocation outlier set relative to the retained syntax and diff-refresh baselines.
- Next-coverage note (2026-04-07): with `merge_open_bootstrap/50k_lines_500_conflicts_streamed` captured, the tracked `merge_open_bootstrap` group now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `diff_refresh_rev_only_same_content/rekey` next; it is the next tracked timing row still reporting `SKIP skipped (not run)` after the merge-bootstrap block, and `target/criterion/diff_refresh_rev_only_same_content/rekey/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, widen into `history_graph/linear_history` rather than revisiting already-covered merge-bootstrap labels.
- Broader-coverage note (2026-04-07): Turn 65 widened exact-run coverage into `diff_refresh_rev_only_same_content/rekey`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_refresh_rev_only_same_content/rekey' --exact` now lands at mean `0.839 ns` with 95% CI `0.837 ns` to `0.842 ns`; sidecar `diff_cache_rekeys=1`, `full_rebuilds=0`, `content_signature_matches=1`, `rows_preserved=5,000`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_refresh_rev_only_same_content/rekey/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:40:02 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 50.000 us`) plus all three structural checks for this label as `OK within budget`, so the tracked `diff_refresh_rev_only_same_content` pair now has fresh exact artifacts for every label on this worktree. This same-content rekey fast path remains far below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): with `diff_refresh_rev_only_same_content/rekey` captured, the tracked `diff_refresh_rev_only_same_content` pair now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `history_graph/linear_history` next; it is the first still-missing tracked timing row after the diff-refresh pair and the right next baseline before `history_graph/merge_dense` and `history_graph/branch_heads_dense`.
- Broader-coverage note (2026-04-07): Turn 66 widened exact-run coverage into `history_graph/linear_history`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_graph/linear_history' --exact` now lands at mean `55.797 us` with 95% CI `54.971 us` to `56.793 us`; sidecar `commit_count=5,000`, `graph_rows=5,000`, `max_lanes=1`, `merge_count=0`, `branch_heads=0`, `alloc_bytes=610,240`, `alloc_ops=2`, `dealloc_ops=2`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/history_graph/linear_history/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:41:41 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1500.000 ms`) plus both structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This linear-history graph build stays comfortably below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `history_graph/linear_history`, stay in the same group and capture `history_graph/merge_dense` next. It is the adjacent tracked history-graph label still reporting `SKIP skipped (not run)` on this worktree, and it is the right follow-up before `history_graph/branch_heads_dense` because it should show whether merge fanout changes the same graph-construction baseline without revisiting already-covered groups.
- Broader-coverage note (2026-04-07): Turn 67 widened exact-run coverage into `history_graph/merge_dense`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_graph/merge_dense' --exact` now lands at mean `392.680 us` with 95% CI `391.240 us` to `394.129 us`; sidecar `commit_count=5,000`, `graph_rows=5,000`, `max_lanes=3`, `merge_count=198`, `branch_heads=0`, `alloc_bytes=1,225,376`, `alloc_ops=5`, `dealloc_ops=5`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/history_graph/merge_dense/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:43:34 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1500.000 ms`) plus the structural `graph_rows == 5000` and `merge_count >= 100` checks as `OK within budget`, so no harness or budget correction was needed. This dense-merge history graph build stays comfortably below budget and does not enter the current timing or allocation outlier set, though it is measurably heavier than the fresh `history_graph/linear_history` control on the same worktree.
- Next-coverage note (2026-04-07): after `history_graph/merge_dense`, stay in the same group and capture `history_graph/branch_heads_dense` next. It is the remaining tracked history-graph timing row still reporting `SKIP skipped (not run)` on this worktree, and it is the right final same-group control before widening into a different uncovered `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 68 finished the tracked `history_graph` timing group with `history_graph/branch_heads_dense`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_graph/branch_heads_dense' --exact` now lands at mean `354.688 us` with 95% CI `353.819 us` to `355.817 us`; sidecar `commit_count=5,000`, `graph_rows=5,000`, `max_lanes=3`, `merge_count=99`, `branch_heads=455`, `alloc_bytes=701,364`, `alloc_ops=15`, `dealloc_ops=15`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/history_graph/branch_heads_dense/new/estimates.json` and `sidecar.json`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1500.000 ms`) plus the structural `graph_rows == 5000` and `branch_heads >= 100` checks as `OK within budget`, so no harness or budget correction was needed. This branch-head-heavy history graph build is slightly lighter than the fresh `history_graph/merge_dense` sibling and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): with `history_graph/branch_heads_dense` captured, the tracked `history_graph` timing group now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `commit_details/many_files` next; it is the first remaining tracked timing row still reporting `SKIP skipped (not run)` after the history-graph block, and `target/criterion/commit_details/many_files/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`.
- Broader-coverage note (2026-04-07): Turn 69 widened exact-run coverage into `commit_details/many_files`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/many_files' --exact` now lands at mean `27.404 us` with 95% CI `27.309 us` to `27.527 us`; sidecar `file_count=5000`, `max_path_depth=5`, `message_bytes=54`, `message_lines=3`, `message_shaped_lines=0`, `added_files=654`, `modified_files=3256`, `deleted_files=218`, `renamed_files=436`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/many_files/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:47:15 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 10.000 ms`) plus both structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This first `commit_details` baseline stays far below budget and does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `commit_details/many_files`, stay in the same group and capture `commit_details/deep_paths` next. It is the adjacent tracked `commit_details` label still reporting `SKIP skipped (not run)` on this worktree, and it is the right path-depth contrast before widening to the heavier `huge_file_list` and `10k_files_depth_12` siblings.
- Broader-coverage note (2026-04-07): Turn 70 widened exact-run coverage into `commit_details/deep_paths`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/deep_paths' --exact` now lands at mean `57.976 us` with 95% CI `57.812 us` to `58.222 us`; sidecar `file_count=5000`, `max_path_depth=17`, `message_bytes=54`, `message_lines=3`, `message_shaped_lines=0`, `message_shaped_bytes=0`, `added_files=654`, `modified_files=3256`, `deleted_files=218`, `renamed_files=436`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/deep_paths/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:48:56 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 15.000 ms`) plus both structural checks for this label as `OK within budget`, so no harness or budget correction was needed. This deep-path sibling stays far below budget; it is about `2.12x` slower than the fresh `commit_details/many_files` control (`57.976 us` vs `27.404 us`) but still does not enter the current timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `commit_details/deep_paths`, stay in the same group and capture `commit_details/huge_file_list` next. It is the next tracked `commit_details` timing row still reporting `SKIP skipped (not run)` on this worktree, and it is the first file-count scale-up control before the large-message, deeper `10k_files_depth_12`, selection-replace, and path-display-cache-churn siblings.
- Broader-coverage note (2026-04-07): Turn 71 widened exact-run coverage into `commit_details/huge_file_list`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/huge_file_list' --exact` now lands at mean `54.928 us` with 95% CI `54.474 us` to `55.611 us`; sidecar `file_count=10000`, `max_path_depth=5`, `message_bytes=54`, `message_lines=3`, `message_shaped_lines=0`, `message_shaped_bytes=0`, `added_files=1305`, `modified_files=6520`, `deleted_files=435`, `renamed_files=870`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/huge_file_list/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:51:00 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 ms`) plus the structural `file_count >= 10000` check as `OK within budget`, so no harness or budget correction was needed. This 10k-file scale-up case stays far below budget and does not enter the current timing or allocation outlier set; it is slightly lighter than the fresh `commit_details/deep_paths` sibling despite doubling file count, which suggests path depth is still the stronger factor in this group than raw file count alone.
- Next-coverage note (2026-04-07): after `commit_details/huge_file_list`, stay in the same group and capture `commit_details/large_message_body` next. It is the adjacent tracked `commit_details` label still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/commit_details/large_message_body/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `commit_details/10k_files_depth_12` before widening into the selection-replace and path-display-cache-churn siblings.
- Broader-coverage note (2026-04-07): Turn 72 widened exact-run coverage into `commit_details/large_message_body`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/large_message_body' --exact` now lands at mean `26.204 us` with 95% CI `26.170 us` to `26.241 us`; sidecar `file_count=5000`, `max_path_depth=5`, `message_bytes=104199`, `message_lines=596`, `message_shaped_lines=48`, `message_shaped_bytes=8068`, `added_files=654`, `modified_files=3256`, `deleted_files=218`, `renamed_files=436`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/large_message_body/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:53:16 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 30.000 ms`) plus the structural `file_count == 5000`, `message_bytes >= 96000`, and `message_shaped_lines >= 48` checks as `OK within budget`, so no harness or budget correction was needed. This large-message variant stays far below budget and does not enter the current timing or allocation outlier set; it is actually slightly lighter than the fresh `commit_details/many_files` control, which suggests commit-message shaping is not a primary cost driver in this group on the current worktree.
- Next-coverage note (2026-04-07): after `commit_details/large_message_body`, stay in the same group and capture `commit_details/10k_files_depth_12` next. It is the next tracked `commit_details` timing row still reporting `SKIP skipped (not run)` on this worktree, it combines the higher file-count and deeper-path stressors, and `target/criterion/commit_details/10k_files_depth_12/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `commit_details/select_commit_replace` before the final `path_display_cache_churn` sibling.
- Broader-coverage note (2026-04-07): Turn 73 widened exact-run coverage into `commit_details/10k_files_depth_12`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/10k_files_depth_12' --exact` now lands at mean `87.170 us` with 95% CI `87.091 us` to `87.270 us`; sidecar `file_count=10000`, `max_path_depth=13`, `message_bytes=54`, `message_lines=3`, `message_shaped_lines=0`, `message_shaped_bytes=0`, `added_files=1305`, `modified_files=6520`, `deleted_files=435`, `renamed_files=870`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/10k_files_depth_12/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:55:23 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 45.000 ms`) plus the structural `file_count == 10000` and `max_path_depth >= 13` checks as `OK within budget`, so no harness or budget correction was needed. This combined file-count-plus-depth `commit_details` case stays far below budget and does not enter the current timing or allocation outlier set, but it is now the heaviest fresh `commit_details` baseline on this worktree at about `3.18x` `many_files`, `1.59x` `huge_file_list`, and `1.50x` `deep_paths`, which suggests the combined scale and depth stressors compound more than either factor alone in this group.
- Next-coverage note (2026-04-07): after `commit_details/10k_files_depth_12`, stay in the same group and capture `commit_details/select_commit_replace` next. It is the adjacent tracked `commit_details` timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/commit_details/select_commit_replace/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, finish the current `commit_details` coverage block with `commit_details/path_display_cache_churn` before widening elsewhere.
- Broader-coverage note (2026-04-07): Turn 74 widened exact-run coverage into `commit_details/select_commit_replace`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/select_commit_replace' --exact` now lands at mean `26.326 us` with 95% CI `26.282 us` to `26.393 us`; sidecar `files_a=5000`, `files_b=5000`, `commit_ids_differ=1`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/commit_details/select_commit_replace/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:57:37 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 20.000 ms`) plus the structural `commit_ids_differ == 1`, `files_a >= 5000`, and `files_b >= 5000` checks as `OK within budget`, so no harness or budget correction was needed. This commit-switch reducer stays in the same low-microsecond / zero-allocation regime as the rest of the current `commit_details` group and does not enter the timing or allocation outlier set.
- Next-coverage note (2026-04-07): after `commit_details/select_commit_replace`, stay in the same group and capture `commit_details/path_display_cache_churn` next. It is now the last tracked `commit_details` timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/commit_details/path_display_cache_churn/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, the current `commit_details` coverage block will be complete and the next turn can widen into a different missing `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 75 finished the tracked `commit_details` timing group with `commit_details/path_display_cache_churn`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'commit_details/path_display_cache_churn' --exact` now lands at mean `483.651 us` with 95% CI `482.551 us` to `485.054 us`; sidecar `file_count=10000`, `path_display_cache_hits=0`, `path_display_cache_misses=10000`, `path_display_cache_clears=1`, `alloc_bytes=985,712`, `alloc_ops=10,000`, `dealloc_ops=1,808`, `realloc_ops=0`, and `net_alloc_bytes=804,048`. The exact run refreshed both `target/criterion/commit_details/path_display_cache_churn/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 03:59:26 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 30.000 ms`) plus the structural `file_count >= 10000`, `path_display_cache_clears >= 1`, and `path_display_cache_misses >= 10000` checks as `OK within budget`, so no harness or budget correction was needed. This is now the heaviest fresh `commit_details` timing baseline and the first allocation-bearing one on this worktree, but it still stays comfortably below budget and does not enter the broader timing or allocation outlier set.
- Next-coverage note (2026-04-07): with `commit_details/path_display_cache_churn` captured, the tracked `commit_details` timing group now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `patch_diff_paged_rows/eager_full_materialize` next; it is the first tracked timing row still reporting `SKIP skipped (not run)` after the `commit_details` block, and `target/criterion/patch_diff_paged_rows/` currently only has stale sidecar-only artifacts for the non-windowed siblings while `patch_diff_paged_rows/paged_first_window/200` still has no Criterion artifact on this worktree.
- Broader-coverage note (2026-04-07): Turn 76 widened exact-run coverage into `patch_diff_paged_rows/eager_full_materialize` on the default `GITCOMET_BENCH_PATCH_DIFF_LINES=20000` synthetic unified diff. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'patch_diff_paged_rows/eager_full_materialize' --exact` now lands at mean `16.072 ms` with 95% CI `16.055 ms` to `16.089 ms`; sidecar `alloc_bytes=16,093,040`, `alloc_ops=114,295`, `dealloc_ops=114,295`, `realloc_ops=1`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/patch_diff_paged_rows/eager_full_materialize/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:02:38 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 200.000 ms`) as `OK within budget`; no structural `patch_diff_paged_rows` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the shape and allocation verification for this group. This eager full-materialization baseline stays well below budget and outside the current cross-suite timing and allocation outlier sets, but it is now the first fresh `patch_diff_paged_rows` measurement on this worktree and the right same-group control to compare against the paged and visible-map siblings.
- Next-coverage note (2026-04-07): after `patch_diff_paged_rows/eager_full_materialize`, stay in the same group and capture `patch_diff_paged_rows/paged_first_window/200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/patch_diff_paged_rows/paged_first_window/200/` still has no Criterion artifact on this worktree. If that passes cleanly, continue with `patch_diff_paged_rows/inline_visible_eager_scan` before the final `inline_visible_hidden_map` sibling.
- Broader-coverage note (2026-04-07): Turn 77 widened exact-run coverage into `patch_diff_paged_rows/paged_first_window/200` on the same default `GITCOMET_BENCH_PATCH_DIFF_LINES=20000` synthetic unified diff. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'patch_diff_paged_rows/paged_first_window/200' --exact` now lands at mean `61.247 us` with 95% CI `60.792 us` to `61.556 us`; sidecar `alloc_bytes=123,756`, `alloc_ops=981`, `dealloc_ops=981`, `realloc_ops=1`, and `net_alloc_bytes=-192`. The exact run refreshed both `target/criterion/patch_diff_paged_rows/paged_first_window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:04:50 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 10.000 ms`) as `OK within budget`; no structural `patch_diff_paged_rows` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the shape and allocation verification for this group. This first-window paging baseline stays well below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh same-group `eager_full_materialize` control, it is about `262x` faster and about `130x` smaller in `alloc_bytes`, which confirms this group's first-window paging cost is not the same hot path as full diff materialization.
- Next-coverage note (2026-04-07): after `patch_diff_paged_rows/paged_first_window/200`, stay in the same group and capture `patch_diff_paged_rows/inline_visible_eager_scan` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/patch_diff_paged_rows/inline_visible_eager_scan/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, finish the current `patch_diff_paged_rows` coverage block with `patch_diff_paged_rows/inline_visible_hidden_map` before widening elsewhere again.
- Broader-coverage note (2026-04-07): Turn 78 widened exact-run coverage into `patch_diff_paged_rows/inline_visible_eager_scan` on the same default `GITCOMET_BENCH_PATCH_DIFF_LINES=20000` synthetic unified diff. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'patch_diff_paged_rows/inline_visible_eager_scan' --exact` now lands at mean `623.890 us` with 95% CI `623.031 us` to `624.589 us`; sidecar `alloc_bytes=2,101,068`, `alloc_ops=188`, `dealloc_ops=188`, `realloc_ops=19`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/patch_diff_paged_rows/inline_visible_eager_scan/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:06:44 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 ms`) as `OK within budget`; no structural `patch_diff_paged_rows` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the shape and allocation verification for this group. This visible-index eager scan stays well below budget and outside the current cross-suite timing and allocation outlier sets, but it is now the middle same-group cost: about `10.2x` slower and about `17.0x` larger in `alloc_bytes` than the fresh `paged_first_window/200` control, while still about `25.8x` faster and about `7.7x` smaller in `alloc_bytes` than `eager_full_materialize`.
- Next-coverage note (2026-04-07): after `patch_diff_paged_rows/inline_visible_eager_scan`, stay in the same group and capture `patch_diff_paged_rows/inline_visible_hidden_map` next. It is now the last tracked `patch_diff_paged_rows` timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/patch_diff_paged_rows/inline_visible_hidden_map/new/` still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, the current `patch_diff_paged_rows` coverage block will be complete and the next turn can widen into a different missing `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 79 finished the tracked `patch_diff_paged_rows` timing group with `patch_diff_paged_rows/inline_visible_hidden_map` on the same default `GITCOMET_BENCH_PATCH_DIFF_LINES=20000` synthetic unified diff. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'patch_diff_paged_rows/inline_visible_hidden_map' --exact` now lands at mean `8.261 us` with 95% CI `8.248 us` to `8.283 us`; sidecar `alloc_bytes=96`, `alloc_ops=1`, `dealloc_ops=1`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/patch_diff_paged_rows/inline_visible_hidden_map/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:09:00 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 50.000 ms`) as `OK within budget`; no structural `patch_diff_paged_rows` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the shape and allocation verification for this group. This hidden-map step is now the lightest same-group cost on this worktree: about `7.4x` faster and about `1,289x` smaller in `alloc_bytes` than `paged_first_window/200`, about `75.5x` faster and about `21,886x` smaller in `alloc_bytes` than `inline_visible_eager_scan`, and about `1,945x` faster and about `167,636x` smaller in `alloc_bytes` than `eager_full_materialize`.
- Next-coverage note (2026-04-07): with `patch_diff_paged_rows/inline_visible_hidden_map` captured, the tracked `patch_diff_paged_rows` timing group now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `diff_open_patch_deep_window_90pct/200` next; it is the first tracked timing row still reporting `SKIP skipped (not run)` after the now-complete `patch_diff_paged_rows` block, and `target/criterion/diff_open_patch_deep_window_90pct/200/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue inside the same diff-open block with `diff_open_markdown_preview_first_window/200` before the image, conflict, and SVG siblings.
- Broader-coverage note (2026-04-07): Turn 80 widened exact-run coverage into `diff_open_patch_deep_window_90pct/200` on the default `GITCOMET_BENCH_PATCH_DIFF_LINES=20000` synthetic unified diff. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_patch_deep_window_90pct/200' --exact` now lands at mean `280.199 us` with 95% CI `279.944 us` to `280.447 us`; sidecar `rows_requested=200`, `rows_painted=200`, `rows_materialized=200`, `patch_rows_painted=200`, `patch_rows_materialized=512`, `patch_page_cache_entries=2`, `full_text_materializations=0`, `alloc_bytes=43,260`, `alloc_ops=7`, `dealloc_ops=7`, `realloc_ops=6`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_open_patch_deep_window_90pct/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:11:39 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 15.000 ms`) as `OK within budget`; no structural `diff_open_patch_deep_window_90pct` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the shape verification for this label. This deep-window first-paint path stays well below budget and outside the current timing and allocation outlier sets; versus the fresh same-input `patch_diff_paged_rows/paged_first_window/200` control, it is about `4.6x` slower but about `2.9x` smaller in `alloc_bytes`, which suggests the deeper page lookup adds latency without creating a comparable allocation hotspot.
- Next-coverage note (2026-04-07): after `diff_open_patch_deep_window_90pct/200`, stay in the same diff-open block and capture `diff_open_markdown_preview_first_window/200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/diff_open_markdown_preview_first_window/200/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `diff_open_image_preview_first_paint` before the remaining patch-100k, conflict, and SVG siblings.
- Broader-coverage note (2026-04-07): Turn 81 widened exact-run coverage into `diff_open_markdown_preview_first_window/200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_markdown_preview_first_window/200' --exact` now lands at mean `413.306 us` with 95% CI `406.427 us` to `422.376 us`; sidecar `old_total_rows=3,840`, `new_total_rows=3,840`, `old_rows_rendered=200`, `new_rows_rendered=200`, `alloc_bytes=3,847,648`, `alloc_ops=1,365`, `dealloc_ops=42`, `realloc_ops=0`, and `net_alloc_bytes=3,837,088`. The exact run refreshed both `target/criterion/diff_open_markdown_preview_first_window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:13:47 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 20.000 ms`) plus the existing structural `old_rows_rendered >= 1` and `new_rows_rendered >= 1` checks as `OK within budget`, so no harness or budget correction was needed. This markdown-preview diff-open path stays well below budget and outside the current cross-suite outlier sets, but versus the fresh `diff_open_patch_deep_window_90pct/200` control it is about `1.48x` slower and about `88.9x` larger in `alloc_bytes`, which makes it the heaviest measured allocation case inside the current diff-open block so far.
- Next-coverage note (2026-04-07): after `diff_open_markdown_preview_first_window/200`, stay in the same diff-open block and capture `diff_open_image_preview_first_paint` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/diff_open_image_preview_first_paint/new/` currently only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `diff_open_patch_100k_lines_first_window/200` before the remaining conflict and SVG siblings.
- Broader-coverage note (2026-04-07): Turn 82 widened exact-run coverage into `diff_open_image_preview_first_paint`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_image_preview_first_paint' --exact` now lands at mean `1.392 ns` with 95% CI `1.391 ns` to `1.392 ns`; sidecar `old_bytes=262,144`, `new_bytes=393,216`, `total_bytes=655,360`, `images_rendered=2`, `placeholder_cells=0`, `divider_count=1`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_open_image_preview_first_paint/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:16:18 +0300`, replacing the older sidecar-only artifact with a credible matched pair. Code inspection confirmed this fixture intentionally prebuilds ready `gpui::Image` handles once and only times the cached two-cell-plus-divider fingerprint step, so the nanosecond timing and zero-allocation sidecar are expected for this control rather than a harness blocker. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 15.000 ms`) plus the structural `old_bytes`, `new_bytes`, `total_bytes`, `images_rendered`, `placeholder_cells`, and `divider_count` checks as `OK within budget`. This cached-preview control stays far outside the current timing and allocation outlier sets and is much lighter than the already-fresh markdown and deep-window diff-open siblings.
- Next-coverage note (2026-04-07): after `diff_open_image_preview_first_paint`, stay in the same diff-open block and capture `diff_open_patch_100k_lines_first_window/200` next. It is the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/diff_open_patch_100k_lines_first_window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `diff_open_conflict_compare_first_window/200` before the remaining SVG sibling.
- Broader-coverage note (2026-04-07): Turn 83 widened exact-run coverage into `diff_open_patch_100k_lines_first_window/200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_patch_100k_lines_first_window/200' --exact` now lands at mean `59.096 us` with 95% CI `58.764 us` to `59.355 us`; sidecar `first_window_ns=18,510`, `rows_requested=200`, `rows_painted=200`, `rows_materialized=200`, `patch_page_cache_entries=1`, `full_text_materializations=0`, `alloc_bytes=20,732`, `alloc_ops=5`, `dealloc_ops=5`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_open_patch_100k_lines_first_window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:18:56 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 30.000 ms`) plus the structural `rows_painted >= 200` and `full_text_materializations == 0` checks as `OK within budget`, so no harness or budget correction was needed. This 100k-line first-window patch-open path stays well below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `diff_open_patch_deep_window_90pct/200` control, it is about `4.7x` faster and about `2.1x` smaller in `alloc_bytes`, which suggests larger patch size alone is still not pushing first-window diff-open paging into an outlier regime on this worktree.
- Next-coverage note (2026-04-07): after `diff_open_patch_100k_lines_first_window/200`, stay in the same diff-open block and capture `diff_open_conflict_compare_first_window/200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/diff_open_conflict_compare_first_window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, finish the current diff-open block with `diff_open_svg_dual_path_first_window/200`.
- Broader-coverage note (2026-04-07): Turn 84 widened exact-run coverage into `diff_open_conflict_compare_first_window/200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_conflict_compare_first_window/200' --exact` now lands at mean `843.267 ns` with 95% CI `842.357 ns` to `844.351 ns`; sidecar `total_diff_rows=2200`, `total_visible_rows=2200`, `rows_rendered=200`, `conflict_count=300`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_open_conflict_compare_first_window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:21:05 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 20.000 ms`) plus the structural `rows_rendered >= 200` and `conflict_count >= 1` checks as `OK within budget`, so no harness or budget correction was needed. This conflict-compare first-window control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `diff_open_patch_100k_lines_first_window/200` control it is about `70.1x` faster and removes the remaining small paging allocation footprint entirely.
- Next-coverage note (2026-04-07): after `diff_open_conflict_compare_first_window/200`, stay in the same diff-open block and capture `diff_open_svg_dual_path_first_window/200` next. It is now the last tracked diff-open timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/diff_open_svg_dual_path_first_window/200/new/` still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, the current diff-open coverage block will be complete and the next turn can widen into a different missing `perf_budget_report` group.
- Broader-coverage note (2026-04-07): Turn 85 finished the tracked diff-open timing block with `diff_open_svg_dual_path_first_window/200`. The first exact run exposed a measurement-credibility gap: `SvgDualPathFirstWindowFixture::measure_first_window()` only returned precomputed metrics, so the sidecar stayed at `alloc_bytes=0` even though the timed path called live `render_svg_image_diff_preview(...)`. The fixture now routes both the sidecar closure and the timed step through a shared `exercise_first_window()` helper, and a fresh exact rerun of `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_open_svg_dual_path_first_window/200' --exact` now lands at mean `2.066 ms` with 95% CI `2.062 ms` to `2.073 ms`; sidecar `old_svg_bytes=9,890`, `new_svg_bytes=65,536`, `rasterize_success=1`, `fallback_triggered=1`, `rasterized_png_bytes=118,388`, `images_rendered=1`, `divider_count=1`, `alloc_bytes=2,956,525`, `alloc_ops=2,026`, `dealloc_ops=2,016`, `realloc_ops=426`, and `net_alloc_bytes=379`. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 ms`) plus all four structural checks as `OK within budget`, so the older zero-allocation sidecar is superseded as a harness artifact. This SVG dual-path control is now the slowest fresh diff-open label on this worktree at about `5.0x` `diff_open_markdown_preview_first_window/200`, but it still stays well below budget and about `1.3x` smaller in `alloc_bytes` than that markdown sibling.
- Next-coverage note (2026-04-07): with `diff_open_svg_dual_path_first_window/200` captured and its sidecar corrected, the tracked diff-open timing block now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `conflict_three_way_scroll/style_window/200` next; it is the first remaining `perf_budget_report` timing row still reporting `SKIP skipped (not run)`, and `target/criterion/conflict_three_way_scroll/style_window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue within the same conflict coverage block with `conflict_two_way_split_scroll/window_200`.
- Broader-coverage note (2026-04-07): Turn 86 widened exact-run coverage into `conflict_three_way_scroll/style_window/200` on the default `GITCOMET_BENCH_CONFLICT_LINES=10000`, `GITCOMET_BENCH_CONFLICT_BLOCKS=300`, and `GITCOMET_BENCH_CONFLICT_WINDOW=200` synthetic three-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_three_way_scroll/style_window/200' --exact` now lands at mean `1.707 ms` with 95% CI `1.630 ms` to `1.763 ms`; sidecar `alloc_bytes=374,696`, `alloc_ops=975`, `dealloc_ops=975`, `realloc_ops=318`, `net_alloc_bytes=4,576`, `tree_sitter_alloc_bytes=370,816`, `tree_sitter_alloc_ops=4,134`, `tree_sitter_dealloc_ops=4,134`, `tree_sitter_realloc_ops=159`, and `tree_sitter_net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_three_way_scroll/style_window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:30:01 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 8.000 ms`) as `OK within budget`; no structural `conflict_three_way_scroll` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the allocation verification for this label. This first tracked conflict-scroll baseline stays well below budget and outside the current cross-suite timing and allocation outlier sets, but it is the first fresh conflict timing artifact on this worktree and confirms nonzero Rust-side plus native parser churn rather than another sidecar-integrity artifact.
- Next-coverage note (2026-04-07): after `conflict_three_way_scroll/style_window/200`, stay in the same conflict coverage block and capture `conflict_two_way_split_scroll/window_200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_two_way_split_scroll/window_200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with the next remaining conflict timing row after it.
- Broader-coverage note (2026-04-07): Turn 87 widened exact-run coverage into `conflict_two_way_split_scroll/window_200` on the same default `GITCOMET_BENCH_CONFLICT_LINES=10000` and `GITCOMET_BENCH_CONFLICT_BLOCKS=300` synthetic two-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_two_way_split_scroll/window_200' --exact` now lands at mean `849.079 ns` with 95% CI `847.999 ns` to `850.438 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_two_way_split_scroll/window_200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:32:26 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 6.000 ms`) as `OK within budget`; no structural `conflict_two_way_split_scroll` checks are wired in `perf_budget_report` today. Fixture inspection shows this control prewarms the split styled-text cache up front and the timed step only hashes cached visible rows plus conflict ids, so the zero-allocation sidecar is expected rather than a fresh harness-integrity bug. This two-way split scroll control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_three_way_scroll/style_window/200` baseline it is about `2,000x` faster and removes the earlier parser/styling churn entirely.
- Next-coverage note (2026-04-07): after `conflict_two_way_split_scroll/window_200`, stay in the same conflict coverage block and capture `conflict_search_query_update/window/200` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_search_query_update/window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_split_resize_step/window/200`.
- Broader-coverage note (2026-04-07): Turn 88 widened exact-run coverage into `conflict_search_query_update/window/200` on the same default `GITCOMET_BENCH_CONFLICT_LINES=10000`, `GITCOMET_BENCH_CONFLICT_BLOCKS=300`, and `GITCOMET_BENCH_CONFLICT_WINDOW=200` synthetic two-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_search_query_update/window/200' --exact` now lands at mean `33.635 us` with 95% CI `33.532 us` to `33.705 us`; sidecar `alloc_bytes=55,088`, `alloc_ops=54`, `dealloc_ops=27`, `realloc_ops=0`, `dealloc_bytes=30,464`, and `net_alloc_bytes=24,624`. The exact run refreshed both `target/criterion/conflict_search_query_update/window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:34:49 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 40.000 ms`) as `OK within budget`; no structural `conflict_search_query_update` checks are wired in `perf_budget_report` today. Fixture inspection shows this control prewarms the stable split styled-text cache up front and the timed step only rebuilds the query overlay cache for the active window, so the modest nonzero allocation churn is expected rather than a harness artifact. This search-query update control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_three_way_scroll/style_window/200` baseline it is about `50.8x` faster while retaining measurable cache-update work unlike the nanosecond two-way split scroll control.
- Next-coverage note (2026-04-07): after `conflict_search_query_update/window/200`, stay in the same conflict coverage block and capture `conflict_split_resize_step/window/200` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_split_resize_step/window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_provider/index_build`.
- Broader-coverage note (2026-04-07): Turn 89 widened exact-run coverage into `conflict_split_resize_step/window/200` on the same default `GITCOMET_BENCH_CONFLICT_LINES=10000`, `GITCOMET_BENCH_CONFLICT_BLOCKS=300`, and `GITCOMET_BENCH_CONFLICT_WINDOW=200` synthetic two-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_split_resize_step/window/200' --exact` now lands at mean `8.962 us` with 95% CI `8.931 us` to `9.018 us`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_split_resize_step/window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:36:47 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 ms`) as `OK within budget`; no structural `conflict_split_resize_step` checks are wired in `perf_budget_report` today. Fixture inspection shows this control keeps a fixed `resize_query` string while only advancing split widths after warm-up, so the query-overlay cache is already hot and the representative zero-allocation sidecar is expected rather than a harness-integrity bug. This split-resize control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_search_query_update/window/200` baseline it is about `3.8x` faster and removes the prior query-overlay allocation churn entirely.
- Next-coverage note (2026-04-07): after `conflict_split_resize_step/window/200`, stay in the same conflict coverage block and capture `conflict_streamed_provider/index_build` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_provider/index_build/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_provider/first_page/200`.
- Broader-coverage note (2026-04-07): Turn 90 widened exact-run coverage into `conflict_streamed_provider/index_build` on the default `GITCOMET_BENCH_STREAMED_LINES=50000` whole-file synthetic conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_provider/index_build' --exact` now lands at mean `4.580 ms` with 95% CI `4.576 ms` to `4.584 ms`; sidecar `alloc_bytes=3,728`, `alloc_ops=2`, `dealloc_ops=2`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_provider/index_build/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:39:29 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 50.000 ms`) as `OK within budget`; no structural `conflict_streamed_provider/index_build` checks are wired in `perf_budget_report` today. Fixture inspection shows this control rebuilds `ConflictSplitRowIndex` from scratch and hashes the resulting total-row count, so the small but nonzero sidecar is expected rather than another cache-hit or sidecar-drift artifact. This streamed-provider index build is now the slowest fresh conflict-path label on this worktree at about `2.7x` `conflict_three_way_scroll/style_window/200`, but it still stays well below budget and its Rust-side allocation footprint is about `100x` smaller than that style-window baseline.
- Next-coverage note (2026-04-07): after `conflict_streamed_provider/index_build`, stay in the same conflict-streamed-provider block and capture `conflict_streamed_provider/first_page/200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_provider/first_page/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_provider/first_page_cache_hit/200`.
- Broader-coverage note (2026-04-07): Turn 91 widened exact-run coverage into `conflict_streamed_provider/first_page/200` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` whole-file synthetic conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_provider/first_page/200' --exact` now lands at mean `15.075 us` with 95% CI `15.000 us` to `15.202 us`; sidecar `alloc_bytes=53,264`, `alloc_ops=2`, `dealloc_ops=2`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_provider/first_page/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:41:43 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) as `OK within budget`; no structural `conflict_streamed_provider/first_page/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows this control clears the page cache before hashing the first visible window, so the small nonzero sidecar is expected first-page materialization work rather than another harness bug. This first-page streamed-provider path stays well below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_provider/index_build` control it is about `304x` faster but about `14.3x` larger in Rust-side `alloc_bytes`, which matches first-page cache population rather than anchor-index rebuild cost.
- Next-coverage note (2026-04-07): after `conflict_streamed_provider/first_page/200`, stay in the same conflict-streamed-provider block and capture `conflict_streamed_provider/first_page_cache_hit/200` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_provider/first_page_cache_hit/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_provider/deep_scroll_90pct/200`.
- Broader-coverage note (2026-04-07): Turn 92 widened exact-run coverage into `conflict_streamed_provider/first_page_cache_hit/200` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` whole-file synthetic conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_provider/first_page_cache_hit/200' --exact` now lands at mean `873.668 ns` with 95% CI `872.050 ns` to `875.574 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_provider/first_page_cache_hit/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:44:52 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 30.000 us`) as `OK within budget`; no structural `conflict_streamed_provider/first_page_cache_hit/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows this control primes the first page cache once before the timed loop and then only re-hashes the same visible window from the warm page cache, so the zero-allocation sidecar is expected rather than another harness bug. This cache-hit streamed-provider control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_provider/first_page/200` baseline it is about `17.3x` faster and removes the earlier `53,264`-byte first-page cache-population cost entirely.
- Next-coverage note (2026-04-07): after `conflict_streamed_provider/first_page_cache_hit/200`, stay in the same conflict-streamed-provider block and capture `conflict_streamed_provider/deep_scroll_90pct/200` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_provider/deep_scroll_90pct/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_provider/search_rare_text`.
- Broader-coverage note (2026-04-07): Turn 93 widened exact-run coverage into `conflict_streamed_provider/deep_scroll_90pct/200` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` whole-file synthetic conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_provider/deep_scroll_90pct/200' --exact` now lands at mean `33.882 us` with 95% CI `32.801 us` to `35.178 us`; sidecar `alloc_bytes=106,528`, `alloc_ops=4`, `dealloc_bytes=79,888`, `dealloc_ops=3`, `realloc_ops=0`, and `net_alloc_bytes=26,640`. The exact run refreshed both `target/criterion/conflict_streamed_provider/deep_scroll_90pct/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:47:14 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 120.000 us`) as `OK within budget`; no structural `conflict_streamed_provider/deep_scroll_90pct/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the allocation verification for this label. Fixture inspection shows `run_deep_scroll_step()` clears cached pages before hashing a late visible window, so the larger nonzero sidecar is expected deep-page materialization work rather than another sidecar-integrity bug. This deep-scroll streamed-provider path stays well below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_provider/first_page/200` baseline it is about `2.2x` slower and `2.0x` larger in Rust-side `alloc_bytes`, which matches the deeper cache-rebuild path rather than a new blocker.
- Next-coverage note (2026-04-07): after `conflict_streamed_provider/deep_scroll_90pct/200`, stay in the same conflict-streamed-provider block and capture `conflict_streamed_provider/search_rare_text` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_provider/search_rare_text/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with the first remaining `conflict_streamed_resolved_output/*` timing row after it.
- Broader-coverage note (2026-04-07): Turn 94 widened exact-run coverage into `conflict_streamed_provider/search_rare_text` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` whole-file synthetic conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_provider/search_rare_text' --exact` now lands at mean `52.378 ns` with 95% CI `52.175 ns` to `52.494 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_provider/search_rare_text/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:49:35 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 200.000 us`) as `OK within budget`; no structural `conflict_streamed_provider/search_rare_text` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the allocation verification for this label. Fixture inspection shows the benchmark searches for `shared_42(` while the synthetic conflict text is built from `let shared_{slot_ix} = ...` lines, and `search_text_matching_rows(...)` returns an empty `Vec` without heap growth on that miss-only path, so the zero-allocation sidecar is expected rather than another integrity bug. This miss-heavy search control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_provider/deep_scroll_90pct/200` baseline it is about `647x` faster and removes the earlier deep-page materialization churn entirely.
- Next-coverage note (2026-04-07): after `conflict_streamed_provider/search_rare_text`, widen exact-run coverage into `conflict_streamed_resolved_output/projection_build` next. It is now the first remaining conflict-path timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_resolved_output/projection_build/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_resolved_output/window/200`.
- Broader-coverage note (2026-04-07): Turn 95 widened exact-run coverage into `conflict_streamed_resolved_output/projection_build` on the default `GITCOMET_BENCH_STREAMED_LINES=50000` and `GITCOMET_BENCH_CONFLICT_BLOCKS=500` synthetic three-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_resolved_output/projection_build' --exact` now lands at mean `345.772 us` with 95% CI `344.306 us` to `348.339 us`; sidecar `alloc_bytes=1,355,784`, `alloc_ops=1,007`, `dealloc_bytes=1,355,784`, `dealloc_ops=1,007`, `realloc_ops=501`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_resolved_output/projection_build/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:52:43 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 5.000 ms`) as `OK within budget`; no structural `conflict_streamed_resolved_output/projection_build` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the allocation verification for this label. Fixture inspection shows `run_projection_build_step()` rebuilds `ResolvedOutputProjection::from_segments(&self.segments)` and hashes the resulting `len()` plus `output_hash()`, so this moderate allocation sidecar is expected projection-metadata construction rather than window materialization. This projection-build baseline stays well below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_provider/index_build` control it is about `13.2x` faster but about `364x` larger in Rust-side `alloc_bytes`, which matches resolved-output projection rebuild being metadata-heavy even when timing remains modest.
- Next-coverage note (2026-04-07): after `conflict_streamed_resolved_output/projection_build`, stay in the same resolved-output block and capture `conflict_streamed_resolved_output/window/200` next. It is now the adjacent tracked timing row still reporting `SKIP skipped (not run)` on this worktree, and `target/criterion/conflict_streamed_resolved_output/window/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `conflict_streamed_resolved_output/deep_window_90pct/200`.
- Broader-coverage note (2026-04-07): Turn 96 widened exact-run coverage into `conflict_streamed_resolved_output/window/200` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` and `GITCOMET_BENCH_CONFLICT_BLOCKS=500` synthetic three-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_resolved_output/window/200' --exact` now lands at mean `528.389 ns` with 95% CI `527.865 ns` to `528.950 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_resolved_output/window/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:54:59 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 us`) as `OK within budget`; no structural `conflict_streamed_resolved_output/window/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `run_window_step()` only hashes the first visible range through the already-built `ResolvedOutputProjection::for_each_line_text_in_range(...)`, so the zero-allocation sidecar is expected steady-state window enumeration rather than another artifact-integrity bug. This resolved-output first-window control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_resolved_output/projection_build` baseline it is about `654x` faster and removes the earlier `1,355,784`-byte projection rebuild cost entirely.
- Next-coverage note (2026-04-07): after `conflict_streamed_resolved_output/window/200`, stay in the same resolved-output block and capture `conflict_streamed_resolved_output/deep_window_90pct/200` next. It is now the last tracked timing row still reporting `SKIP skipped (not run)` in this block, and `target/criterion/conflict_streamed_resolved_output/deep_window_90pct/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with the next remaining timing row still `SKIP` in `perf_budget_report`, currently `markdown_preview_parse_build/single_document/medium`.
- Broader-coverage note (2026-04-07): Turn 97 widened exact-run coverage into `conflict_streamed_resolved_output/deep_window_90pct/200` on the same default `GITCOMET_BENCH_STREAMED_LINES=50000` and `GITCOMET_BENCH_CONFLICT_BLOCKS=500` synthetic three-way conflict fixture. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'conflict_streamed_resolved_output/deep_window_90pct/200' --exact` now lands at mean `530.937 ns` with 95% CI `530.677 ns` to `531.181 ns`; sidecar `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/conflict_streamed_resolved_output/deep_window_90pct/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:57:18 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 us`) as `OK within budget`; no structural `conflict_streamed_resolved_output/deep_window_90pct/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `run_deep_window_step()` only computes a late start offset inside the already-built `ResolvedOutputProjection` and hashes that visible range through `for_each_line_text_in_range(...)`, so the zero-allocation sidecar is expected steady-state deep-window enumeration rather than another artifact-integrity bug. This deep-window resolved-output control stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `conflict_streamed_resolved_output/window/200` baseline it is only about `1.005x` slower and retains the same zero-allocation shape.
- Next-coverage note (2026-04-07): with `conflict_streamed_resolved_output/deep_window_90pct/200` captured, the tracked `conflict_streamed_resolved_output` block now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `markdown_preview_parse_build/single_document/medium` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/markdown_preview_parse_build/single_document/medium/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`.
- Broader-coverage note (2026-04-07): Turn 98 widened exact-run coverage into `markdown_preview_parse_build/single_document/medium` using the benchmark defaults `GITCOMET_BENCH_MARKDOWN_PREVIEW_MEDIUM_SECTIONS=256` and `GITCOMET_BENCH_MARKDOWN_PREVIEW_LINE_BYTES=128`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_parse_build/single_document/medium' --exact` now lands at mean `1.135 ms` with 95% CI `1.134 ms` to `1.136 ms`; sidecar `alloc_bytes=3,031,160`, `alloc_ops=8,972`, `dealloc_bytes=3,031,160`, `dealloc_ops=8,972`, `realloc_ops=292`, `realloc_bytes_delta=1,128,656`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/markdown_preview_parse_build/single_document/medium/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 04:59:30 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 2.000 ms`) as `OK within budget`; no structural `markdown_preview_parse_build/single_document/medium` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `MarkdownPreviewFixture::new(256, 128)` rebuilds a synthetic single markdown document and `run_parse_single_step()` re-runs `markdown_preview::parse_markdown(&self.single_source)` before hashing the parsed document, so the measured churn is expected parse-build work rather than a sidecar-integrity bug. This medium single-document markdown parse baseline stays below budget and outside the current cross-suite timing and allocation outlier sets; it is materially heavier than the nanosecond warm window controls nearby, but still far lighter than the retained large-JSON syntax and diff-refresh outliers.
- Next-coverage note (2026-04-07): after `markdown_preview_parse_build/single_document/medium`, stay in the same markdown-preview parse block and capture `markdown_preview_parse_build/two_sided_diff/medium` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/markdown_preview_parse_build/two_sided_diff/medium/new/` still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `markdown_preview_render_single/window_rows/200`.
- Broader-coverage note (2026-04-07): Turn 99 widened exact-run coverage into `markdown_preview_parse_build/two_sided_diff/medium` using the same benchmark defaults `GITCOMET_BENCH_MARKDOWN_PREVIEW_MEDIUM_SECTIONS=256` and `GITCOMET_BENCH_MARKDOWN_PREVIEW_LINE_BYTES=128`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_parse_build/two_sided_diff/medium' --exact` now lands at mean `3.928 ms` with 95% CI `3.904 ms` to `3.962 ms`; sidecar `alloc_bytes=16,954,306`, `alloc_ops=43,069`, `dealloc_bytes=16,954,306`, `dealloc_ops=43,069`, `realloc_ops=638`, `realloc_bytes_delta=5,597,309`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/markdown_preview_parse_build/two_sided_diff/medium/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:01:44 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 500.000 ms`) as `OK within budget`; no structural `markdown_preview_parse_build/two_sided_diff/medium` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `run_parse_diff_step()` re-runs `markdown_preview::build_markdown_diff_preview(&self.old_source, &self.new_source)` and hashes both preview documents, so the heavier sidecar is expected two-sided parse-build work rather than another sidecar-integrity bug. This medium markdown diff-parse baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets, though it is about `3.46x` slower and `5.59x` heavier in `alloc_bytes` than the fresh `markdown_preview_parse_build/single_document/medium` control.
- Next-coverage note (2026-04-07): after `markdown_preview_parse_build/two_sided_diff/medium`, stay in the same markdown-preview block and capture `markdown_preview_render_single/window_rows/200` next. It is now the first tracked timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/markdown_preview_render_single/window_rows/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `markdown_preview_render_diff/window_rows/200`.
- Broader-coverage note (2026-04-07): Turn 100 widened exact-run coverage into `markdown_preview_render_single/window_rows/200` using the benchmark defaults `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_SECTIONS=384`, `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_LINE_BYTES=128`, and `GITCOMET_BENCH_MARKDOWN_PREVIEW_WINDOW=200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_render_single/window_rows/200' --exact` now lands at mean `216.694 us` with 95% CI `213.653 us` to `220.181 us`; sidecar `alloc_bytes=2,431,072`, `alloc_ops=543`, `dealloc_bytes=5,280`, `dealloc_ops=21`, `realloc_ops=0`, and `net_alloc_bytes=2,425,792`. The exact run refreshed both `target/criterion/markdown_preview_render_single/window_rows/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:04:21 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 1.500 ms`) as `OK within budget`; no structural `markdown_preview_render_single/window_rows/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `run_render_single_step(start, window)` hashes the `Vec<AnyElement>` returned by `render_markdown_preview_window(...)` against the prebuilt `single_document`, so this sidecar is expected first-window GPUI element construction rather than another artifact-integrity bug. This single-document markdown render baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `markdown_preview_parse_build/single_document/medium` control it is about `5.24x` faster and about `1.25x` lighter in `alloc_bytes`.
- Next-coverage note (2026-04-07): after `markdown_preview_render_single/window_rows/200`, stay in the same markdown-preview render block and capture `markdown_preview_render_diff/window_rows/200` next. It is now the next tracked timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/markdown_preview_render_diff/window_rows/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `markdown_preview_scroll/window_rows/200`.
- Broader-coverage note (2026-04-07): Turn 101 widened exact-run coverage into `markdown_preview_render_diff/window_rows/200` using the same benchmark defaults `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_SECTIONS=384`, `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_LINE_BYTES=128`, and `GITCOMET_BENCH_MARKDOWN_PREVIEW_WINDOW=200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_render_diff/window_rows/200' --exact` now lands at mean `430.132 us` with 95% CI `423.856 us` to `437.444 us`; sidecar `alloc_bytes=3,813,568`, `alloc_ops=1,085`, `dealloc_bytes=10,560`, `dealloc_ops=42`, `realloc_ops=0`, and `net_alloc_bytes=3,803,008`. The exact run refreshed both `target/criterion/markdown_preview_render_diff/window_rows/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:06:30 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 2.500 ms`) as `OK within budget`; no structural `markdown_preview_render_diff/window_rows/200` checks are wired in `perf_budget_report` today, so the fresh sidecar remains the only allocation verification for this label. Fixture inspection shows `run_render_diff_step(start, window)` renders both old and new windows from the prebuilt markdown diff preview and hashes the rendered lengths, so the heavier sidecar is expected dual-pane GPUI element construction rather than another artifact-integrity bug. This markdown diff-render baseline stays below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `markdown_preview_render_single/window_rows/200` control it is about `1.99x` slower and about `1.57x` heavier in `alloc_bytes`.
- Next-coverage note (2026-04-07): after `markdown_preview_render_diff/window_rows/200`, stay in the same markdown-preview scroll block and capture `markdown_preview_scroll/window_rows/200` next. It is now the first tracked timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/markdown_preview_scroll/window_rows/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. If that passes cleanly, continue with `markdown_preview_scroll/rich_5000_rows_window_rows/200`, whose target directory is also still in the same stale sidecar-only state.
- Broader-coverage note (2026-04-07): Turn 102 widened exact-run coverage into `markdown_preview_scroll/window_rows/200` using the markdown scroll defaults `GITCOMET_BENCH_MARKDOWN_PREVIEW_SCROLL_SECTIONS=768`, `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_LINE_BYTES=128`, `GITCOMET_BENCH_MARKDOWN_PREVIEW_WINDOW=200`, and `GITCOMET_BENCH_MARKDOWN_PREVIEW_SCROLL_STEP=24`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_scroll/window_rows/200' --exact` now lands at mean `209.145 us` with 95% CI `202.809 us` to `217.837 us`; sidecar `total_rows=7,680`, `start_row=24`, `window_size=200`, `rows_rendered=200`, `scroll_step_rows=24`, `heading_rows=768`, `list_rows=1,536`, `table_rows=1,536`, `code_rows=1,536`, `blockquote_rows=768`, `alloc_bytes=1,382,496`, `alloc_ops=542`, `dealloc_bytes=5,280`, `dealloc_ops=21`, `realloc_ops=0`, and `net_alloc_bytes=1,377,216`. The exact run refreshed both `target/criterion/markdown_preview_scroll/window_rows/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:08:44 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 2.000 ms`) plus the structural `total_rows`, `start_row`, `window_size`, `rows_rendered`, and `scroll_step_rows` checks as `OK within budget`. Fixture inspection shows `run_scroll_step(start, window)` hashes `hash_markdown_preview_window(self.theme, &self.document, start, window)` over the pre-parsed markdown document, so this sidecar is expected steady-state preview-window materialization rather than parse-build churn or another artifact-integrity bug. This scroll baseline stays below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `markdown_preview_render_single/window_rows/200` control it is about `4.2%` faster and about `43.1%` lighter in `alloc_bytes` (`1,048,576` fewer bytes).
- Next-coverage note (2026-04-07): after `markdown_preview_scroll/window_rows/200`, stay in the same markdown-preview scroll block and capture `markdown_preview_scroll/rich_5000_rows_window_rows/200` next. It is now the remaining tracked timing row still reporting `SKIP skipped (not run)` in this block, and `target/criterion/markdown_preview_scroll/rich_5000_rows_window_rows/200/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json`. Its structural rows are already wired and currently read from that stale sidecar-only artifact, so the next exact run should refresh the full matched pair before widening beyond markdown-preview coverage.
- Broader-coverage note (2026-04-07): Turn 103 finished the tracked markdown-preview scroll block with `markdown_preview_scroll/rich_5000_rows_window_rows/200` using the same default `GITCOMET_BENCH_MARKDOWN_PREVIEW_RENDER_LINE_BYTES=128`, `GITCOMET_BENCH_MARKDOWN_PREVIEW_WINDOW=200`, and `GITCOMET_BENCH_MARKDOWN_PREVIEW_SCROLL_STEP=24` wiring already present in the shared benchmark group. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'markdown_preview_scroll/rich_5000_rows_window_rows/200' --exact` now lands at mean `228.900 us` with 95% CI `226.601 us` to `231.833 us`; sidecar `total_rows=5,000`, `long_rows=500`, `long_row_bytes=2,000`, `start_row=24`, `window_size=200`, `rows_rendered=200`, `scroll_step_rows=24`, `heading_rows=500`, `list_rows=1,000`, `table_rows=750`, `code_rows=1,000`, `blockquote_rows=500`, `details_rows=250`, `alloc_bytes=2,469,632`, `alloc_ops=643`, `dealloc_bytes=11,760`, `dealloc_ops=51`, `realloc_ops=0`, and `net_alloc_bytes=2,457,872`. The exact run refreshed both `target/criterion/markdown_preview_scroll/rich_5000_rows_window_rows/200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:11:59 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 25.000 ms`) plus the structural `total_rows`, `long_rows`, `long_row_bytes`, `start_row`, `window_size`, `rows_rendered`, `scroll_step_rows`, `heading_rows`, `list_rows`, `table_rows`, `code_rows`, `blockquote_rows`, and `details_rows` checks as `OK within budget`. Fixture inspection shows `MarkdownPreviewScrollFixture::new_rich_5000_rows()` prebuilds a rich 5,000-row synthetic markdown document with the documented long-row mix up front, and `run_scroll_step(start, window)` only hashes rendered preview rows over that already-built document, so this sidecar is expected steady-state rich-window materialization rather than parse-build churn or another artifact-integrity bug. This rich-scroll baseline stays below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh plain `markdown_preview_scroll/window_rows/200` control it is about `9.4%` slower and about `78.6%` heavier in `alloc_bytes` (`1,087,136` more bytes), which matches the long-row rich-content fixture rather than a new blocker.
- Next-coverage note (2026-04-07): with `markdown_preview_scroll/rich_5000_rows_window_rows/200` captured, the tracked markdown-preview timing block now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `pane_resize_drag_step/sidebar` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/pane_resize_drag_step/sidebar/new/` currently still only has a stale `sidecar.json` without a matching `estimates.json` even though its structural rows already read as populated.
- Broader-coverage note (2026-04-07): Turn 104 widened exact-run coverage into `pane_resize_drag_step/sidebar`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'pane_resize_drag_step/sidebar' --exact` now lands at mean `974.171 ns` with 95% CI `973.096 ns` to `975.452 ns`; sidecar `steps=200`, `width_bounds_recomputes=200`, `layout_recomputes=200`, `min_pane_width_px=200.0`, `max_pane_width_px=564.0`, `min_main_width_px=280.0`, `max_main_width_px=644.0`, `clamp_at_min_count=6`, `clamp_at_max_count=6`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/pane_resize_drag_step/sidebar/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:14:33 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `steps`, `width_bounds_recomputes`, `layout_recomputes`, `clamp_at_min_count`, and `clamp_at_max_count` checks as `OK within budget`. Fixture inspection shows `PaneResizeDragStepFixture::run_with_metrics()` only replays 200 production pane clamp/layout recomputes, so the zero-allocation sidecar is expected arithmetic-only drag math rather than another artifact-integrity bug. This sidebar resize-step baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets.
- Next-coverage note (2026-04-07): after `pane_resize_drag_step/sidebar`, stay in the same resize block and capture `pane_resize_drag_step/details` next. It is the adjacent tracked timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/pane_resize_drag_step/details/new/` still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated.
- Broader-coverage note (2026-04-07): Turn 105 widened exact-run coverage into `pane_resize_drag_step/details`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'pane_resize_drag_step/details' --exact` now lands at mean `1.012 us` with 95% CI `1.011 us` to `1.013 us`; sidecar `steps=200`, `width_bounds_recomputes=200`, `layout_recomputes=200`, `min_pane_width_px=280.0`, `max_pane_width_px=704.0`, `min_main_width_px=280.0`, `max_main_width_px=704.0`, `clamp_at_min_count=6`, `clamp_at_max_count=5`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/pane_resize_drag_step/details/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:16:51 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `steps`, `width_bounds_recomputes`, `layout_recomputes`, `clamp_at_min_count`, and `clamp_at_max_count` checks as `OK within budget`. Fixture inspection shows the same `PaneResizeDragStepFixture::run_with_metrics()` path only replays 200 production pane clamp/layout recomputes for the details pane, so the zero-allocation sidecar is expected arithmetic-only drag math rather than another artifact-integrity bug. This details resize-step baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `pane_resize_drag_step/sidebar` control it is about `3.9%` slower (`37.5 ns`) because of the wider pane-width span, but it still shows no allocation churn.
- Next-coverage note (2026-04-07): with `pane_resize_drag_step/details` captured, the tracked `pane_resize_drag_step` timing pair now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `diff_split_resize_drag_step/window_200` next; it is the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/diff_split_resize_drag_step/window_200/new/` currently still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated.
- Broader-coverage note (2026-04-07): Turn 106 widened exact-run coverage into `diff_split_resize_drag_step/window_200`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'diff_split_resize_drag_step/window_200' --exact` now lands at mean `859.870 ns` with 95% CI `859.273 ns` to `860.539 ns`; sidecar `steps=200`, `ratio_recomputes=200`, `column_width_recomputes=200`, `min_ratio=0.28776979446411133`, `max_ratio=0.7122302055358887`, `min_left_col_px=160.0`, `max_left_col_px=396.0`, `min_right_col_px=160.0`, `max_right_col_px=396.0`, `clamp_at_min_count=5`, `clamp_at_max_count=5`, `narrow_fallback_count=0`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/diff_split_resize_drag_step/window_200/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:19:49 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `steps`, `ratio_recomputes`, `column_width_recomputes`, `clamp_at_min_count`, `clamp_at_max_count`, `narrow_fallback_count`, `min_ratio`, and `max_ratio` checks as `OK within budget`. Fixture inspection shows `DiffSplitResizeDragStepFixture::run_with_metrics()` only replays 200 diff-split ratio and column-width recomputes, so the zero-allocation sidecar is expected arithmetic-only drag math rather than another artifact-integrity bug. This diff-split resize-step baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `pane_resize_drag_step/sidebar` and `pane_resize_drag_step/details` controls it is about `11.7%` and `15.0%` faster respectively while retaining zero allocation churn.
- Next-coverage note (2026-04-07): with `diff_split_resize_drag_step/window_200` captured, widen exact-run coverage into `window_resize_layout/sidebar_main_details` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/window_resize_layout/sidebar_main_details/new/` currently still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated. If that passes cleanly, continue with `history_column_resize_drag_step/branch` before widening into a different still-missing timing group.
- Broader-coverage note (2026-04-07): Turn 107 widened exact-run coverage into `window_resize_layout/sidebar_main_details`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'window_resize_layout/sidebar_main_details' --exact` now lands at mean `171.930 ns` with 95% CI `171.704 ns` to `172.213 ns`; sidecar `steps=200`, `layout_recomputes=200`, `min_main_w_px=84.0`, `max_main_w_px=1079.0`, `clamp_at_zero_count=0`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/window_resize_layout/sidebar_main_details/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:22:23 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `steps`, `layout_recomputes`, and `clamp_at_zero_count` checks as `OK within budget`. Fixture inspection shows `WindowResizeLayoutFixture::run_with_metrics()` only replays 200 `pane_content_width_for_layout(...)` recomputes across a fixed width sweep, so the zero-allocation sidecar is expected arithmetic-only layout math rather than another artifact-integrity bug. This resize-layout baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `diff_split_resize_drag_step/window_200` control it is about `5.0x` faster while retaining zero allocation churn.
- Next-coverage note (2026-04-07): with `window_resize_layout/sidebar_main_details` captured, the tracked `window_resize_layout` timing pair now has fresh exact artifacts for every label on this worktree. Widen exact-run coverage into `history_column_resize_drag_step/branch` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/history_column_resize_drag_step/branch/new/` currently still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated. If that passes cleanly, continue with `history_column_resize_drag_step/graph` before widening into a different still-missing timing group.
- Broader-coverage note (2026-04-07): Turn 108 widened exact-run coverage into `history_column_resize_drag_step/branch`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_column_resize_drag_step/branch' --exact` now lands at mean `1.021 us` with 95% CI `1.020 us` to `1.021 us`; sidecar `steps=200`, `width_clamp_recomputes=200`, `visible_column_recomputes=200`, `columns_hidden_count=0`, `clamp_at_min_count=9`, `clamp_at_max_count=9`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/history_column_resize_drag_step/branch/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:24:33 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `steps`, `width_clamp_recomputes`, `visible_column_recomputes`, and `clamp_at_max_count` checks as `OK within budget`. Fixture inspection shows `HistoryColumnResizeDragStepFixture::run_with_metrics()` only replays 200 width-clamp and visible-column recomputes across the production `history_visible_columns` math, so this zero-allocation sidecar is expected arithmetic-only resize math rather than another artifact-integrity bug. This history-column branch baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; versus the fresh `diff_split_resize_drag_step/window_200` control it is about `18.7%` slower (`160.9 ns`) while retaining zero allocation churn.
- Next-coverage note (2026-04-07): with `history_column_resize_drag_step/branch` captured, stay in the same history-column block and widen exact-run coverage into `history_column_resize_drag_step/graph` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/history_column_resize_drag_step/graph/new/` currently still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated. If that passes cleanly, continue with `history_column_resize_drag_step/author` before widening into a different still-missing timing group.
- Broader-coverage note (2026-04-07): Turn 109 widened exact-run coverage into `history_column_resize_drag_step/graph`. `cargo bench -p gitcomet-ui-gpui --features benchmarks --bench performance -- --noplot 'history_column_resize_drag_step/graph' --exact` now lands at mean `1.023 us` with 95% CI `1.023 us` to `1.024 us`; sidecar `steps=200`, `width_clamp_recomputes=200`, `visible_column_recomputes=200`, `columns_hidden_count=0`, `clamp_at_min_count=14`, `clamp_at_max_count=15`, `alloc_bytes=0`, `alloc_ops=0`, `dealloc_ops=0`, `realloc_ops=0`, and `net_alloc_bytes=0`. The exact run refreshed both `target/criterion/history_column_resize_drag_step/graph/new/estimates.json` and `sidecar.json` timestamped `2026-04-07 05:27:05 +0300`, replacing the older sidecar-only artifact with a credible matched pair. A follow-up `cargo run -p gitcomet-ui-gpui --bin perf_budget_report -- --skip-missing` now reports the timing row (`<= 100.000 us`) plus the structural `width_clamp_recomputes` and `visible_column_recomputes` checks as `OK within budget`. Fixture inspection shows the same `HistoryColumnResizeDragStepFixture::run_with_metrics()` path only replays 200 width clamps and visible-column recomputes across the production `history_visible_columns` math, so this zero-allocation sidecar is expected arithmetic-only resize math rather than another artifact-integrity bug. This history-column graph baseline stays far below budget and outside the current cross-suite timing and allocation outlier sets; it uses about `1.023%` of budget, is only about `0.22%` (`2.250 ns`) slower than the fresh `history_column_resize_drag_step/branch` control, and remains about `18.97%` slower (`163.157 ns`) than the fresh `diff_split_resize_drag_step/window_200` control while keeping the same zero-allocation shape.
- Next-coverage note (2026-04-07): with `history_column_resize_drag_step/graph` captured, stay in the same history-column block and widen exact-run coverage into `history_column_resize_drag_step/author` next; it is now the first remaining timing row still reporting `SKIP skipped (not run)` in `perf_budget_report`, and `target/criterion/history_column_resize_drag_step/author/new/` currently still only has a stale `sidecar.json` timestamped `2026-04-07 00:47:35 +0300` without a matching `estimates.json` even though its structural rows already read as populated. If that passes cleanly, continue with `history_column_resize_drag_step/date` before widening into a different still-missing timing group.
